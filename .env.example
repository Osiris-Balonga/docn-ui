# Public ingestion credentials. The project token is intentionally browser-visible
# and can only send events; it does not grant analytics query access.
NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=
NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com

