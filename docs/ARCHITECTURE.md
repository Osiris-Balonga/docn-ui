# Target architecture

Status: target architecture with the L01 bootstrap implemented and in review. The two workspaces, static root/404 routes, shadcn primitives, local site fonts, and scoped quality tooling exist. PDF, catalog, registry, and editor modules below are still planned. Detailed decisions are in the [ADRs](adr/0001-stack.md).

## Stack selected for the plan

| Layer | Choice | Reason |
| --- | --- | --- |
| Runtime/tooling | Compatible Node LTS, pnpm workspace, strict TypeScript | Reproducibility and two clear scopes |
| Site | Next.js App Router, static export, React | Indexable catalog/documentation pages, no server required |
| Interface | shadcn/ui, Base UI, Tailwind CSS, Lucide | User requirement and source ownership |
| Generation | `@react-pdf/renderer` | Composable PDF primitives, browser/Node |
| PDF display | `pdfjs-dist`, local worker | Display generated bytes, pages, selectable text |
| Validation | Zod | Data schemas and structured messages |
| QR | Maintained encoder to qualify, vector rendering | Readability, no third-party request |
| Verification | Vitest, Testing Library, Playwright, axe, PDF inspection tools | Behavior and actual artifacts |
| Documentation | Local MDX, controlled components | Versioned content, no CMS |
| Distribution | shadcn-compatible registry JSON | Reuse its CLI; no proprietary V1 CLI |

L01 resolves and records exact compatible stable versions. Do not infer major versions from this document. L02 validates bundler/worker/engine compatibility before generalization. The `pdfjs-dist` viewer and `@react-pdf/renderer` generator are separate libraries; do not confuse them with the viewer wrapper named `react-pdf`.

## Planned directory structure

```text
apps/www/
  src/app/                    # static routes, metadata, layouts
  src/components/ui/          # generated shadcn sources
  src/features/catalog/       # filters and gallery
  src/features/playground/    # forms, state, rendering coordination
  src/features/pdf-viewer/    # PDF.js, text layer, navigation, zoom
  src/features/docs/          # navigation, code, MDX components
  src/content/docs/           # internal MDX guides
  src/lib/catalog/            # lightweight index without PDF engine imports
  src/workers/                # browser rendering and protocol
  src/styles/                 # site tokens
  public/generated/           # build-generated thumbnails/manifests, ignored
  public/r/                   # generated registry, ignored
  public/assets/              # public assets copied from licensed sources
packages/documents/
  src/core/                   # units, formats, geometry, money, results
  src/themes/                 # PDF tokens and validation
  src/primitives/             # Text, Stack, QR, Table... PDF only
  src/templates/<family>/<id>/# composition, schema, examples, metadata
  src/catalog/                # manifest without heavy imports
  src/render/                 # separate browser/node entries, asset resolution
  assets/                     # static fonts and licensed examples
  tests/                      # shared fixtures and PDF integration
tooling/registry/              # manifest, validation, generation, source files
tooling/assets/                # licenses, checksums, copying, thumbnails
tests/consumers/               # Vite/browser and Node, outside-workspace isolation
tests/e2e/                    # built-site journeys
tests/visual/                 # selected references and configuration
docs/                         # plan, decisions, evidence
```

Start with two workspaces only. `packages/documents` stays private on npm: it organizes source code instead of imposing a runtime dependency on users. Add a shared package only when a real dependency justifies it.

## Dependency rules

`core` depends on neither React nor the browser. `themes` depends on `core`. `primitives` depends on `core/themes` and the engine. `templates` depends on those layers, never on the site. `render` adapts templates to the browser or Node. The site imports lightweight metadata and explicitly loads template code on demand.

The registry build reads these same sources. It maintains no second implementation in `apps/www`. Form interfaces live in the site with shared JSON metadata; they are not inserted into distributed PDF sources.

## Rendering flow

```text
Fields/JSON -> validation -> serializable request {revision, templateId, data, format, theme, assets}
            -> generation worker -> PDF + diagnostics + revision
            -> accepted result stored in memory
               -> PDF.js (copy bytes before transfer)
               -> download (same content)
```

Do not send React components, functions, File objects, local paths, or DOM references through the protocol. Normalize to permitted data and buffers. The worker loads templates through a known loader index; never import from a user-provided URL.

One active render and one pending request replaced by the latest. Use a short 250 ms debounce; ignore stale results. Logical cancellation does not stop CPU work: terminate and recreate the worker when needed on timeout or navigation. Do not spawn unlimited workers.

The viewer may transfer/detach buffers: keep immutable download bytes and give it a copy. Revoke Object URLs and destroy PDF.js loading/render tasks on document changes and unmount. For long documents, display only pages near the viewport.

## Static Next.js and boundaries

Enumerate dynamic routes with `generateStaticParams`; no arbitrary runtime slugs. Generate metadata and documentation at build time. Read public filter parameters on the client; retain a static shell/Suspense boundary compatible with export. Server Components pass only serializable metadata, never a template component or Zod schema.

No Server Actions, generation API, request-dependent middleware, authentication cookies, ISR, or server image optimizer. Generate catalog images at build time and serve local files; explicitly configure an export-compatible mode. Security headers come from the host, not a Next API unavailable in static export.

Serve matching versions of PDF.js/generator workers, fonts, and any required CMaps locally. L02 proves production resolution, not just `dev` resolution.

## Asset generation

Template sources → demo PDFs with fixed data → rasterization → thumbnails → metadata/registry → site build. No separately drawn thumbnails. A manifest connects source hashes, fixtures, fonts, engine version, and derived assets. Changes invalidate affected artifacts; large outputs remain CI artifacts rather than Git files.

The build must prepare assets before Next copies `public`. Examples and licenses required for installation without the docn-ui network are included in the distribution path described by [REGISTRY.md](specs/REGISTRY.md).

## Extensions without premature implementation

Schemas carry `schemaVersion`; template and registry versions are explicit. This supports evolution without requiring a universal engine. A hosted API or visual editor would need a separate PRD and ADRs, particularly for privacy and security.
