# QA — L00 — Governance and first documentation commit

Date: 2026-08-28. Status: **verified_local**. Story commit: `37d29f056bccb192edf30b889134d215b572968b` (`docs(plan): establish docn-ui implementation baseline`). Active branch after bootstrap: `dev`.

This is a historical report of L00 execution. Later GitHub setup and prerequisites are recorded in [L00G](L00G.md); translating this report does not change its original results.

## Scope

L00-S01 / G0. Local startup authorized by the maintainer. Git initialization, ignore rules, text attributes, and editor configuration. Product content and the then-existing 17 lots were preserved. No application code, installed dependencies, or started service.

The already configured Git identity was used unchanged. No remote added. No commands executed in `paint-3d`.

## Verification

Environment: Windows / PowerShell, Git 2.55.0.windows.2, Node v24.18.0.

| Command or check | Actual result | Evidence |
| --- | --- | --- |
| Directory inspection and initial `git status` | Documentation directory, no preexisting repository | 40 planning files; Git initially reported no repository |
| `git init --initial-branch=main` | Passed | Repository initialized in `docn-ui/.git` |
| One-off Node documentation check before this report | Passed | 43 files, including 39 Markdown files; 189 resolved local links; 17 lots, 56 stories with consistent IDs, commits, dependencies, acceptance, and verification |
| File inventory and secret-signature scan | Passed within the inspected scope | No unexpected file; no private-key, GitHub-token, or AWS-key signature detected; this targeted check is not an exhaustive guarantee |
| `git check-ignore` | Rules for dependencies, builds, secrets, artifacts, generated registry | `.env.example` and `.env.production.example` are not ignored |
| `git remote -v` | No remote | No remote destination configured |
| `git commit -m 'docs(plan): establish docn-ui implementation baseline'` | Passed | Root commit `37d29f0`, 44 files, documentation/governance only |
| `git switch -c dev`, then `git rev-parse main dev` | Passed | Both refs initially equal `37d29f056bccb192edf30b889134d215b572968b` |
| `git status --short --branch` after creating `dev` | Clean tree | Output `## dev`, no changed files |

The first `git diff --cached --check` found trailing blank lines in 13 existing lot files. They were removed without content changes; the second check and the check immediately before committing passed. `git check-ignore` assertions verified 13 ignored and 3 permitted paths. `git check-attr` confirmed LF for Markdown, CRLF for CMD, and binary PDF handling.

## Justification for added tests

No application test added or run: no application behavior changed. The one-off documentation validator ran through Node without installing dependencies or creating a permanent suite. No `pnpm test`, PDF, browser, consumer, or visual command was relevant to this lot.

## Visual and document checks

Not applicable to L00. No screen, PDF, screenshot, print quality, or user journey claimed as verified.

## Deviations and resumption

The follow-up documentation commit `docs(progress): record verified governance baseline` records the existing SHA and branch checks without amendment. This traceability requirement was documented in L00 before execution. It is on `dev`; `main` retains the initial commit. Its SHA was to be added to `actualCommits` in the next lot update to avoid amendment cycles.

At the end of L00, L01 remained `planned`: the resumption protocol allowed one lot per request unless local continuation was explicitly authorized. CI, merging, publication, license, and a remote destination were out of scope. Next.js/shadcn bootstrap and separate quality commands were then the next planned step; the later maintainer request inserted L00G before L01.
