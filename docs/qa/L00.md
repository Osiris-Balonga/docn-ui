# QA — L00 — Gouvernance et premier commit documentaire

Date : 2026-08-28. Statut : **verified_local**. Commit de story : `37d29f056bccb192edf30b889134d215b572968b` (`docs(plan): establish docn-ui implementation baseline`). Branche active après bootstrap : `dev`.

## Périmètre

L00-S01 / G0. Démarrage local autorisé par le mainteneur. Initialisation Git, règles d'ignore, attributs texte et configuration éditeur. Le contenu produit et les 17 lots existants sont conservés. Aucun code applicatif, dépendance installée ou service lancé.

L'identité Git déjà configurée est utilisée sans modification. Aucun remote ajouté. Aucune commande exécutée dans `paint-3d`.

## Vérifications

Environnement : Windows / PowerShell, Git 2.55.0.windows.2, Node v24.18.0.

| Commande ou contrôle | Résultat réel | Preuve |
| --- | --- | --- |
| Inspection du dossier et `git status` initial | Dossier documentaire, aucun dépôt préexistant | 40 fichiers de planification ; Git indique initialement l'absence de dépôt |
| `git init --initial-branch=main` | Réussi | Dépôt initialisé dans `docn-ui/.git` |
| Contrôle Node ponctuel des documents avant ce rapport | Réussi | 43 fichiers, dont 39 Markdown ; 189 liens locaux résolus ; 17 lots, 56 stories avec IDs, commits, dépendances, acceptation et vérification cohérents |
| Inventaire des fichiers et recherche de signatures de secrets | Réussi sur le périmètre contrôlé | Aucun fichier inattendu ; aucune signature de clé privée, token GitHub ou clé AWS détectée ; ce contrôle ciblé ne constitue pas une garantie exhaustive |
| `git check-ignore` | Règles présentes pour dépendances, builds, secrets, artefacts et registre généré | Exemples `.env.example` et `.env.production.example` non ignorés |
| `git remote -v` | Aucun remote | Aucune destination distante configurée |
| `git commit -m 'docs(plan): establish docn-ui implementation baseline'` | Réussi | Commit racine `37d29f0`, 44 fichiers, uniquement documentation et gouvernance |
| `git switch -c dev` puis `git rev-parse main dev` | Réussi | Deux références initialement égales à `37d29f056bccb192edf30b889134d215b572968b` |
| `git status --short --branch` après création de `dev` | Arbre propre | Sortie `## dev`, aucun fichier modifié |

Le premier `git diff --cached --check` a détecté des lignes vides finales dans 13 fiches existantes. Elles ont été retirées sans changer leur contenu ; le second contrôle a réussi, ainsi que celui immédiatement avant le commit. Les assertions `git check-ignore` ont vérifié 13 chemins ignorés et 3 chemins autorisés. `git check-attr` confirme LF pour Markdown, CRLF pour CMD et traitement binaire des PDF.

## Justification des tests ajoutés

Aucun test applicatif ajouté ou exécuté : aucun comportement applicatif ne change. Le validateur documentaire a été exécuté ponctuellement via Node, sans installer de dépendance ni créer une suite permanente. Aucune commande `pnpm test`, PDF, navigateur, consumer ou visuelle n'est pertinente pour ce lot.

## Visuel et document

Sans objet pour L00. Aucun écran, PDF, capture, qualité d'impression ou parcours utilisateur annoncé comme vérifié.

## Écarts et reprise

Le commit documentaire de suivi `docs(progress): record verified governance baseline` enregistre le SHA existant et les vérifications des branches, sans amendement. Cette nécessité de traçabilité est documentée dans la fiche L00 avant exécution. Il est porté par `dev` ; `main` conserve le commit initial. Son propre SHA sera ajouté à `actualCommits` lors de la mise à jour du prochain lot, pour éviter un cycle d'amendements.

L01 reste `planned` : le protocole de reprise prévoit un lot par demande, sauf autorisation explicite d'enchaînement local. CI, fusion, publication, licence et destination distante restent hors périmètre. L'étape suivante est le bootstrap Next.js/shadcn et les commandes de qualité séparées.
