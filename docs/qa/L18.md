# QA — L18 — Unified Node and browser rendering facade

Status: in progress. Branch: `feat/unified-render-facade`. Base SHA:
`67b8d2577dab3332e4eca51f8c242e4b8bfe6278`.

Issue #85 remains open and its Project item
`PVTI_lAHOCZDjCM4BhyZuzg6rbDs` remains In progress. L18-S01 and L18-S02 are
implemented by recorded commits. L18-S03 and L18-S04 are implemented and
locally tested; the current S04 replay correction is intentionally not
self-recorded. S05 qualification remains pending. No deployment, publication,
merge, or release is claimed.

## L18-S01 scope

The Node `renderPdf` facade validates the flat L17 input, validates an exact
runtime-options object, verifies the complete local font manifest by real-path
containment, size, and SHA-256, prepares bounded local images, constructs the
source-owned plan, dispatches fixed/flow plans through the existing finalizer,
and returns the released six-field `RenderResult`. Continuous plans now route a
facade-only measurement coordinator; the stable V1 Node adapter remains
byte-identical.

The input normalizer now uses private prepare/complete phases. The public
`normalizeTemplateInput` behavior remains synchronous and unchanged, while the
runtime continuation performs image preflight after the one permitted schema
parse. The continuation lives in a non-exported internal module and is absent
from both the root barrel and public `./template-contract` subpath. Theme-input presence is
captured in prepared immutable state before any runtime await, so caller
mutation cannot change the legacy-style decision. Implementation commit
`d6cef29b184784eda0b31aea6c2d7e49049f1e48` and corrective commits through
`b0559d1af564988a306e37081f446bd81c8ee251` are recorded in status; the
current corrective commit carrying this review evidence is intentionally not
self-recorded.

## L18-S02 scope

The `./browser` entry now exports the same flat `renderPdf` signature while
retaining the advanced fixed-document exports. Browser runtime options are an
exact plain-object boundary. The font base defaults to
`globalThis.location.origin`; explicit bases must remain on the same HTTP(S)
origin and cannot contain credentials. Each manifest font is fetched with
redirects disabled, rejects a mismatched declared length, is streamed under the
exact manifest byte bound, verified by SHA-256, snapshotted as a data URI, and
temporarily prioritized under a facade mutex. Existing advanced font ordering
is restored in `finally`; stale trusted load state from `Font.reset()` is
rearmed in place without source growth.

Validated local-image IDs are resolved only through the runtime resolver. The
shared bounded preflight owns its byte copies; the browser scope creates
runtime-owned Blob URLs, rolls back partial construction, and revokes every URL
once on success or failure. Result bytes are copied before return and remain
usable for independent caller preview/download copies after resource cleanup.
The direct S02 call has no cancellation, supersession, timeout, navigation, or
worker-termination event. L18-S04 adds those cleanup hooks through the separate
protocol V2 coordinator; they are not retrofitted onto the direct call.

L18-S02 implementation commit
`5abf45cfcc33e1560cb3974c2009a0122ad4920a` and review fixes
`4da9a34a58f610725c71de07129f465d5ca9546c` and
`78caa1438b4a87e3e5ab671c24063127eb516071` are recorded in status.

L18-S03 implementation commit
`f3e9f830b88ee8a1b9c92662bab5cd410f2c32f8` and first review correction
`8c85006953076c913d4e0df13e09c46a770b2d04` and boundary correction
`918017cfabbedb2db389c6387b2b6c3b30ecda14` and shared marker correction
`7c8a4a284566a802e8e29c4b4b3a3a90428bce06` and transformed-bound correction
`6faff5ae109b625ec22b1f3d1d15a9d856049e1c` and marker-contract correction
`c232c5a7a2f276216f2519927e8b1c2d8f06042a` are also recorded. The current
follow-up correction is intentionally not self-recorded.

## L18-S04 scope

Protocol V2 is an opt-in interactive browser coordinator; the direct Node and
browser `renderPdf` calls remain available and protocol V1 remains unchanged.
The V2 request carries the exact flat normalized JSON value, complete resolved
theme, caller revision, validated font identity and pure image descriptors.
`themeWasExplicit` is captured before asynchronous preparation so the worker
preserves the differential legacy-style semantics without rereading caller
input. Image bytes travel as fresh transferable copies in a separate sorted
channel keyed by canonical ID and digest; transfer detachment cannot mutate the
preflight-owned bytes.

The coordinator advances its ownership epoch at submit time and applies a 250
ms latest-only debounce before preflight. It admits one active render plus only
the latest pending render and matches responses by the worker-owned job ID,
caller revision and worker-local epoch. Supersession, the bounded timeout,
navigation and disposal terminate the current worker; later accepted work gets
a new module worker. Late preflight completions and worker results cannot update
the retained result. A separate bounded dispatch ID binds the request and image
envelopes. The worker accepts only strictly increasing dispatch IDs. A replay
matching the pending dispatch/job/revision invalidates and releases that pending
request with one structured `INVALID_DATA` failure; neither image half can then
claim it. An older dispatch or unrelated identity is ignored and cannot
invalidate a newer request. `lastValid` and `stale` are coordinator snapshot fields,
not additions to `RenderResult`.

A resolver already executing cannot be forcibly aborted. Its public promise is
settled on timeout or supersession, but the coordinator keeps that one physical
preflight slot blocked until the resolver returns. The single latest pending
request has its own deadline measured from enqueue and therefore cannot wait
forever; further submissions receive a structured timeout while
`preflightBlocked` is true. The late settle releases prepared copies and
restores acceptance of fresh increasing revisions.

The worker uses a literal trusted ID-to-loader map for the one L17 renderable
catalog adapter. It performs no second Zod/schema parse and receives no
template, plan function, schema, resolver, runtime option, `URL` or other
platform object. It validates exact bounded envelopes and normalized structural
invariants, rechecks image MIME/dimensions/length/SHA-256 from owned transferred
bytes, recomputes the normalized fingerprint, and publishes only an exact
bounded result of at most 20 MiB and 50 pages. Invalid requests and unexpected
worker failures return fixed redacted issues; malformed results, worker errors,
message errors and synchronous `postMessage` failures are terminal.
The coordinator deliberately omits `fontAssetBaseUrl`; that option remains on
direct `renderPdf`, while the static worker resolves root-relative manifest
fonts from `self.location.origin` without transferring runtime options.

Tracking now records the preceding S03 evidence commits
`eb109e49a5d3cdcf735b529c4ba5e761ab093ee2` and
`ad3cd81db0226c037d24fa4e08ab1c37b6cbd1b6`. The S04 implementation commit
`3ba2364f0a3dcdfbaeb92b2d57acc41672ab44e4` and first review correction
`e9e442ab9c6b80b7b0eca841e5eae9962a830c42` are also recorded. The bounded
preflight scheduling and replay correction
`f8b81da1750b907484a4cfc323a3a36c46cfd227` is recorded by the next review
correction. The post-fingerprint and dispatch-correlation correction
`7434c0dbaf6ab288c22cd0f11e398c860ba60f97` is recorded by this follow-up. The
monotonic-dispatch correction
`993d544e843f7f5eac7bcf1dd7d67c1fcfed6093` is recorded by the next follow-up.
The current review-correction commit carrying the latest evidence is
intentionally not self-recorded.

### S04 replay correction verification — 2026-09-13

The inherited uncommitted correction closes a reproducible crossed-halves race:
R1/A, R2/B with the same identity, I2/B, then I1/A. The real worker handler now
publishes one structured failure and performs no render. The regression runs
both identical and conflicting duplicates, repeats late messages, and proves
that the next greater dispatch renders successfully. The inbox test separately
proves that lower IDs and different job/revision identities cannot invalidate
the current pending request. The engine and font I/O are stubbed only in the
handler unit; protocol parsing, inbox state and handler publication are real.

Executed in the dedicated L18 worktree on Windows with Node 24.18.0,
pnpm 11.24.0 and Vitest 4.1.11:

- `corepack pnpm@11.24.0 exec vitest run --project unit packages/documents/src/render/worker-protocol-v2.test.ts packages/documents/src/render/browser-render-coordinator.test.ts`: passed, 2 files / 20 tests.
- `corepack pnpm@11.24.0 exec vitest run --project consumers tests/consumers/browser-render-facade.consumer.test.ts`: passed, 1 Vite/Chromium case, including the production V2 worker.
- `corepack pnpm@11.24.0 --filter @docn-ui/documents typecheck`: passed after replacing a readonly descriptor mutation in the new test with a copied descriptor.
- `corepack pnpm@11.24.0 verify:registry`: passed, 62 items / 5 assets.
- Targeted ESLint and Prettier for the three changed TypeScript files and status JSON, JSON parsing, `git diff --check`, and `git cat-file -e 993d544e843f7f5eac7bcf1dd7d67c1fcfed6093^{commit}`: passed.

Shared image/facade imports did not change, so their suites were not repeated
for this correction. No full suite or visual reference update was needed.
Independent final S04 review and push follow this corrective commit.

## L18-S03 scope

The matching facades now dispatch the same bounded continuous feasibility plan.
The browser path parses the probe with a real package-local PDF.js worker rather
than importing a worker bundle on the caller thread or falling back to a fake
worker. Every loading task, `PDFWorker`, and underlying `Worker` is destroyed in
`finally`. The local worker entry relies on PDF.js's single dedicated-worker
bootstrap and does not register a second handler. A fixed `error` or
`messageerror` signal races document, page, and text loading; it removes both
listeners and terminates every worker resource without waiting on a stalled
loading-task disposal. The Node and browser facade-only measurers return the raw extent of
the complete final glyph bounds; the stable shared coordinator adds ADR 0003's
single 12 pt allowance. Stable V1 runtime, Node, browser, and
feasibility-adapter sources remain unchanged.

Continuous plans require an opaque `^[A-Z][A-Z0-9_]{0,31}$` final-marker token,
their exact continuous format, and the screen profile. The source-owned plan
renders the token as final standalone non-wrapping text, and the facade rejects
invalid tokens before document rendering. After the probe and final render, the
facade reopens the final PDF and requires exactly one page, the qualified width
and bounded height, and a true terminal marker that is an exact item or coherent
same-line PDF.js item sequence with a strict leading boundary, is the last and
lowest relevant text, and has every transformed glyph quadrilateral inside the
MediaBox. Probe and final qualification share this exact terminal-sequence rule.
The released `RenderResult` path then
reports the actual final page count and per-page point/mm dimensions for fixed,
flow, and continuous plans. Unexpected plan construction, renderer,
measurement, print-box, or result-inspection failures cross the facade as a
fixed `RENDER_FAILED` issue at `document`; existing `DocumentValidationError`
codes and paths are preserved. No raw exception text, marker, or document data
enters that fallback issue.

## Image and dependency evidence

`@pdf-lib/upng@1.0.1` (MIT) and `jpeg-js@0.4.4` (BSD-3-Clause) are exact direct
dependencies. The preflight checks source size and MIME signatures before full
pixel decode, validates every PNG chunk CRC, caps inflate at the exact declared
raster budget, limits JPEG decoder resolution and memory, rejects animated PNG,
and applies bounded EXIF orientations 1–8. PNG and rotated JPEG pixels are
deterministically re-encoded as metadata-free PNG. An unrotated JPEG is fully
decoded for validation, then retains its compressed scan while APP metadata and
comments are stripped; this prevents valid compact photos from expanding past
the byte limit. The normalized byte size, final pixel dimensions, MIME, and
SHA-256 form the descriptor. Resolver bytes and Node render-scope bytes are
copied independently; disposal is idempotent.

Node font preparation resolves and verifies each contained path once, then
converts the verified bytes to an immutable WOFF data source. The facade-owned
React PDF registration therefore consumes a digest-bound snapshot. Under the
pinned engine's process-global first-match store, the facade ensures a trusted
canonical source exists for every manifest slot, promotes it only for the
awaited render, and restores prior ordering in `finally`. The tests cover both
API call orders, a cached mismatched source, post-render advanced behavior, and
`Font.reset()`. Source counts remain stable across initial/repeated facade
renders, three reset/render cycles, and an advanced-first canonical data URI
that had already populated the shared registration cache. Facade renders are
serialized; concurrent mixed advanced/facade rendering and destructive
`Font.clear()` are outside the supported process-local contract.

The published npm unpacked sizes are 707,597 bytes and 76,029 bytes
respectively. The isolated codec entry measured 90,065 bytes minified / 31,617
bytes gzip, split as 70,328 / 23,724 for UPNG plus its `(MIT AND Zlib)` pako
1.0.11 dependency and 20,241 / 8,478 for jpeg-js. No stable budget is inferred.
At S02 correction commit `78caa1438b4a87e3e5ab671c24063127eb516071`,
the isolated fixture's `write: false` Vite measurement emitted
30,332 bytes (29.62 KiB; 11,952 bytes / 11.67 KiB gzip), 155,674 bytes
(152.03 KiB; 45,774 bytes / 44.70 KiB gzip), and 1,754,913 bytes
(1,713.78 KiB; 647,440 bytes / 632.27 KiB gzip). This is observed fixture
output, not a stable budget. The site does not import either new facade.
Registry source closure remains unchanged by explicit scope decision and will
be created for the new public workflow in L22.

At marker-contract commit `c232c5a7a2f276216f2519927e8b1c2d8f06042a`, the S03 fixture's exact
`write: false` Vite 8.2.2 measurement used `gzipSync` over each emitted output.
The lazy continuous chunk measured 485,850 bytes / 146,057 bytes gzip and its
dedicated PDF.js worker measured 1,218,699 / 376,763. The other emitted
JavaScript chunks measured 1,562,200 / 597,131, 207,644 / 53,669, 136,425 /
40,029, 30,369 / 11,968, and 19,225 / 6,511 bytes (raw / gzip). The fixture HTML
measured 610 / 325. The continuous engine and worker remain separate lazy chunks
and do not enter the initial fixed-render module. These are a snapshot of that
commit's observed fixture outputs, not budgets; S03 adds no dependency.

## Verification

| Check                        | Result                     | Evidence                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------------------------- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Documents package typecheck  | Passed                     | Command output observed locally                                                                                                                                                                                                                                                                                                                                                               |
| Local-image unit test        | Passed: 1 file, 8 tests    | CRC/inflate bounds, asymmetric EXIF 2–8 plus 1/9, post-scan EXIF, scan-aware JPEG stripping, trailing rejection, strict resolver snapshots, ownership and disposal                                                                                                                                                                                                                            |
| Verified-font unit test      | Passed: 1 file, 1 test     | A font changed after verification cannot alter the digest-bound data source consumed by React PDF                                                                                                                                                                                                                                                                                             |
| Node facade PDF test         | Passed: 4 files, 7 tests   | Public package subpath, fixed/flow results, both font-registration call orders, canonical-cache ownership, priority, restoration, stable counts, and reset recovery                                                                                                                                                                                                                           |
| S03 Node facade PDF test     | Passed: 1 file, 7 tests    | Fixed, flow, and continuous actual results; real PDF.js qualification of 28- and 32-character marker tokens; single 12 pt allowance; final divergence rejection                                                                                                                                                                                                                               |
| Direct Node ESM UPNG probe   | Passed                     | Namespace depth 2 resolved; a 2×1 PNG traversed encode/decode outside Vitest transformation                                                                                                                                                                                                                                                                                                   |
| Immutable V1 registry check  | Passed: 62 items, 5 assets | Legacy Node modules restored byte-for-byte; no source-manifest widening                                                                                                                                                                                                                                                                                                                       |
| Targeted ESLint and Prettier | Passed: no warnings        | Touched S03 facade, measurement, evidence, and fixture files                                                                                                                                                                                                                                                                                                                                  |
| `git diff --check`           | Passed                     | Current S03 diff                                                                                                                                                                                                                                                                                                                                                                              |
| Documentation link verifier  | Not run to completion      | `apps/www/out` is absent in this isolated worktree; S01 did not rebuild the static site                                                                                                                                                                                                                                                                                                       |
| Browser facade unit tests    | Passed: 3 files, 9 tests   | Exact options, origin/credential rejection, resolver requirement, bounded font streams and headers, digest checks, mutex/error restoration, object-URL ownership/rollback                                                                                                                                                                                                                     |
| S03 facade boundary unit     | Passed: 1 file, 4 tests    | Existing option/image boundaries plus bounded marker rejection and constant redacted fallback errors                                                                                                                                                                                                                                                                                          |
| S03 final-qualification unit | Passed: 1 file, 13 tests   | Probe/final parity for exact and coherent same-line fragments; prefix/suffix/spatial collisions and transformed bounds outside every MediaBox edge rejected                                                                                                                                                                                                                                   |
| S03 worker-failure unit      | Passed: 1 file, 3 tests    | `error`, `messageerror`, and ordinary loading rejection release listeners and worker resources even when loading-task disposal remains pending                                                                                                                                                                                                                                                |
| Browser package fixture      | Passed: 1 file, 1 test     | Vite 8.2.2 build and one Chromium case: 5 actual renders across initial/repeat and 3 reset cycles, stable 8-source count, 2 pages at 85 × 55 mm, parity and same-origin requests                                                                                                                                                                                                              |
| S03 browser package fixture  | Passed: 1 file, 1 test     | One Chromium case rendered fixed, flow, and continuous plans; continuous fingerprint and geometry matched Node; PDF.js ran in a separate worker                                                                                                                                                                                                                                               |
| V2 protocol/coordinator unit | Passed: 2 files, 18 tests  | Exact bounded JSON, PNG/JPEG transfer integrity, monotonic dispatch state, crossed-image replay isolation, post-fingerprint epoch gating, 250 ms latest-only debounce, bounded blocked-preflight recovery, active-worker supersession/timeout/error/messageerror/post failure cleanup, stale/late-result rejection, navigation idempotency, exact options and explicit failure discrimination |
| S04 browser worker fixture   | Passed: 1 file, 1 test     | A real Vite module worker rendered the trusted fixed template; revision 102 alone published after supersession, last-valid state became stale, and timeout/pagehide promises rejected                                                                                                                                                                                                         |
| Browser build dependency     | Recorded                   | Vite 8.2.2 is a test-only direct dev dependency already present transitively through Vitest; it does not enter the documents package or production browser bundle                                                                                                                                                                                                                             |

## Limitations

S01-S04 do not expose the facade from the package root, change protocol V1, or
migrate additional catalog templates. Protocol V2 currently loads only the
single trusted L17 renderable adapter; expanding that static map follows later
template migration. The PDF.js worker is internal measurement infrastructure,
not protocol V2. Direct-call cleanup covers success and failure; interruptible
cleanup is available through the separate S04 coordinator. The production
worker map currently contains only a fixed template, so nested protocol-V2 plus
continuous PDF.js-worker termination is deferred until the continuous catalog
migration expands that static map in L20; it is not claimed by the S04 Chromium
evidence. Existing advanced Node/browser render functions and plan types remain
available. The package entry is qualified
through the repository's TypeScript-aware test loader; raw Node TypeScript
stripping is not supported because extensionless source imports are unchanged.
