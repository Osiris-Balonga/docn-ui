# QA — L17 — Unified template and theme contract

Status: in progress. Branch: `feat/unified-template-api`. Base SHA: `c82d3f1d73e8ce864d0669125bc3df0746ff8b3f`.

Issue: #82. Project item: `PVTI_lAHOCZDjCM4BhyZuzg6qepU`. Project status: In progress. Priority: P0. No PR or merge exists for this lot. Last local tracking update: 2026-09-12.

## Scope

L17-S01 specified the unified API contract. L17-S02 adds the non-React template descriptor, the canonical 18-template ID inventory, pure input normalization, geometry-safe custom themes, local-image descriptors, manifest-derived font identity, and complete normalized fingerprints. It preserves the released protocol-V1 request, validator, fingerprint, React template definitions, and advanced theme calls.

The planned S02 implementation is commit `7eeaa0510670558ba20a03bd94a246f07bb962dc`. Value-stability review findings are addressed by `6731572d47eff27f42cc3179109e6e98f6673585`; the following corrective commit adds strict structural validation at the manifest-identity boundary and is intentionally not self-recorded. Binary image preflight, renderable plans, worker protocol V2, and `renderPdf` remain outside S02.

## Verification

| Command or check                                                                                                                      | Environment                                      | Actual result                           | Evidence                                              |
| ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | --------------------------------------- | ----------------------------------------------------- |
| `corepack pnpm@11.24.0 --filter @docn-ui/documents typecheck`                                                                         | Windows, package TypeScript configuration        | Passed                                  | Command output observed locally                       |
| `corepack pnpm@11.24.0 exec vitest run packages/documents/src/template-contract.test.ts packages/documents/src/themes/themes.test.ts` | Windows, Vitest 4.1.11                           | Passed: 2 files, 17 tests               | Command output observed locally                       |
| Prettier on S02 source, tests, lot, QA, and tracking files                                                                            | Windows, Prettier 3.9.6                          | Passed; files are formatted             | Command output observed locally                       |
| `corepack pnpm@11.24.0 verify:docs`                                                                                                   | Windows, source checkout without a static export | Not available: `apps/www/out` is absent | Command stopped with `ENOENT`; no docs result claimed |
| `git diff --check`                                                                                                                    | Windows, Git worktree                            | Passed                                  | Command output observed locally                       |

## Justification for added tests

One focused contract test file covers risks unique to the new boundary: exact catalog identity coverage, one-pass schema transforms, required caller data, structured paths, discriminated formats, continuous-profile rejection, exact image descriptor sets, custom-theme geometry and font qualification, serialized manifest identity authenticity, frozen descriptor defaults, cross-runtime deterministic ordering, normalized fingerprint completeness, and legacy protocol/theme compatibility. Existing theme tests are reused. Renderer, PDF, browser, and visual suites are not repeated because S02 adds no composition or rendering behavior.

## Visual and document checks

Not applicable to S02. No PDF composition, rendered fixture, layout, preview, export, or visual reference changed.

## Deviations and resumption

The implementation keeps binary preflight in L18 as required by the final contract and S02 scope. The S02 checklist wording records that boundary explicitly. Post-commit reviews identified provenance-based manifest validation, locale-sensitive ordering, a mutable descriptor default, and insufficient structural parsing of manifest identities. Corrective commits replace those behaviors with value-based digest validation, code-unit ordering, cloned/frozen defaults, and strict parsing that rejects extra or malformed fields with structured issues. L17 remains `in_progress`; this evidence does not claim local lot verification, review, merge, deployment, or publication.
