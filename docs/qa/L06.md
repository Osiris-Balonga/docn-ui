# QA — L06 — catalog and reusable editor

Date: 2026-08-30. Status: **in progress**. Branch `feat/catalog-and-playground`, base `3947b6fb50518e63c6934d67d73ba15d1ffdad9c`. Issue [#8](https://github.com/Osiris-Balonga/docn-ui/issues/8), Project item `PVTI_lAHOCZDjCM4BhyZuzg4fVaU` in `In progress`. No PR or merge commit exists yet.

## Scope

L06 will add the public searchable catalog, extract a reusable editor shell around explicit family forms, validate bounded advanced JSON, handle safe local image imports and render cleanup, and verify local-only export privacy with stale-result protection.

## Verification

| Command or check | Environment | Actual result | Evidence |
| --- | --- | --- | --- |
| `corepack pnpm test:pdf business-card` with artifact output | Windows, Node 24.18.0, pnpm 11.24.0, Poppler rasterizer | Passed: one existing family file, three cases | Generated the three nominal PDFs; page 1 of each was rasterized at 144 DPI and its SHA-256 recorded in the lightweight catalog manifest |
| `corepack pnpm test:unit catalog-filter` | Windows, Vitest 4.1.11, Node environment | Passed: one focused filter case | Combined search/family/format behavior, empty compatibility, and public query serialization |
| Documents and site type checks plus targeted ESLint | Windows, TypeScript 5.9.3, ESLint 9.39.5 | Passed | Catalog manifest, static routes, gallery, navigation, and journey sources |
| `corepack pnpm test:e2e catalog.spec.ts` | Playwright 1.61.1, Chromium 149, isolated static build/server | Passed: one journey in 35.3 seconds, browser step 1.8 seconds | Three results, search, empty/clear, format URL, known detail route, and browser-back restoration |

## Justification for added tests

Planned coverage is limited to catalog filtering, editor coordination, shared image validation, lifecycle cleanup, and one real catalog/playground journey. Existing schema, PDF, and business-card journeys will be reused instead of multiplying format, theme, MIME, or template permutations.

## Visual and document checks

The minimal, editorial, and Studio nominal PDFs were generated through the existing qualified Node renderer. Their first pages were rasterized at 144 DPI to 482×312, 510×283, and 504×288 PNG files, preserving each actual card proportion. The three files were inspected directly: distinct structures, accents, text, and card edges remain clear with no clipping. Their exact dimensions, source fixtures, page numbers, and hashes are recorded in the serializable catalog manifest; the gallery imports no React PDF composition or renderer.

## Deviations and resumption

L05 PR #25 merged into `dev` at `2026-08-29T23:08:15Z` with merge commit `3947b6fb50518e63c6934d67d73ba15d1ffdad9c`; issue #7 closed and its Project item moved to `Done`. L06 starts exactly from that observed merge. The first catalog E2E attempt asserted the detail heading before navigation had completed, so `goBack()` returned to Playwright's blank initial entry; awaiting the known detail URL corrected the test without changing product behavior. Resume at L06-S02.
