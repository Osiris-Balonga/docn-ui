# QA — L16 — local v1 candidate qualification and public beta evidence

Date: 2026-09-12. Status: **verified locally at synchronized candidate
`c9a4023087fc403e3aa593570b391b1612ed6141`; not delivered or released**.
Active preparation branch: `codex/l16-v1-candidate`, started at
`6207e72bde54453069ef69bfa3f048556dd595ca` and synchronized with
`origin/dev` merge `49fa97ee3a709be122298ad5dbaa3fa693c15b72`. Git ancestry confirms that
this `origin/dev` commit is an ancestor of the candidate. This evidence commit
cannot record its own SHA inside itself; the candidate SHA and every preceding
actual L16 commit are recorded without amending history.

Issue [#20](https://github.com/Osiris-Balonga/docn-ui/issues/20) remains open;
Project item `PVTI_lAHOCZDjCM4BhyZuzg4fVt4` remains In progress. The public-beta
sequence consists of preparation [PR #54](https://github.com/Osiris-Balonga/docn-ui/pull/54),
corrective [PR #55](https://github.com/Osiris-Balonga/docn-ui/pull/55), beta
record [PR #56](https://github.com/Osiris-Balonga/docn-ui/pull/56), MIT
application [PR #57](https://github.com/Osiris-Balonga/docn-ui/pull/57), and
licensed-beta record [PR #58](https://github.com/Osiris-Balonga/docn-ui/pull/58).
Later same-repository `dev -> main` PRs
[#60](https://github.com/Osiris-Balonga/docn-ui/pull/60),
[#62](https://github.com/Osiris-Balonga/docn-ui/pull/62), and
[#64](https://github.com/Osiris-Balonga/docn-ui/pull/64) promoted individual
beta improvements with `release-approved`. They were not an official v1.0.0
delivery and their authorization does not carry over to a future release.

## S01 scope

S01 prepares truthful release documentation only. The candidate release notes
now describe eighteen templates across six families: four invoices, three
receipts, three resumes, three reports, three badges, and two business cards.
The inventory matches the eighteen current template definitions. The README,
PRD, template catalog, testing guidance, risk register, and release procedure
use the same current count while historical L11 evidence remains unchanged.

The maintainer-selected license remains MIT, copyright 2026 Emmanuel Osiris
Balonga. The root and installed registry notices are identical. Font, image,
generated-content, and dependency licenses remain separate obligations. No
license file required a content change in S01.

The changelog deliberately labels v1.0.0 as Unreleased. It documents actual
features, declared formats, beta migration, and limitations without creating a
versioned registry path, changing package versions, or claiming candidate
qualification.

## S01 targeted verification

| Command or check | Environment | Actual result | Evidence |
| --- | --- | --- | --- |
| `corepack pnpm@11.24.0 verify:templates` | Windows, Node 24.18.0, pnpm 11.24.0 | Passed | Verified 18 template PDFs and 21 preview pages. Existing non-fatal PDF.js standard-font warnings remained unchanged. |
| `corepack pnpm@11.24.0 verify:registry` | Same | Passed | Verified 62 registry items against the pinned official schema and 5 local asset files. |
| `corepack pnpm@11.24.0 verify:assets` | Same | Passed | Verified 5 local registry asset files, their tracked licenses, and 74,518 bytes. |
| `corepack pnpm@11.24.0 exec vitest run --project unit tooling/registry/registry.test.ts` | Vitest 4.1.11 | Passed, 1 file / 4 cases | Registry inventory and closure checks include the root-equivalent `~/docn/LICENSE` notice in installed source. |
| Changed-document local-link and JSON parse check | PowerShell 7 | Passed | Resolved 38 relative Markdown links and parsed both changed tracking files. |
| Public origin and version paths | Anonymous HTTPS | Beta state confirmed | Home and `/r/dev/registry.json` returned 200; `/r/v1.0.0/registry.json` returned 404; `robots.txt` returned `Disallow: /`. This is expected evidence that official delivery is incomplete. |
| GitHub PR/tag/release read | GitHub CLI, read-only | Historical facts confirmed | PR #58 and interim promotions #60/#62/#64 are merged. The repository has no tag and no GitHub Release. |
| `corepack pnpm@11.24.0 verify:docs` | Exact candidate static output | Passed | Verified 44 public pages and 2,562 local links, including anchors. The earlier S01-only attempt stopped because the isolated worktree did not yet contain `apps/www/out`; the candidate build supplied that prerequisite. |

## PR #69 brand and analytics integration evidence

This preflight covered the product changes at
`6207e72bde54453069ef69bfa3f048556dd595ca` before candidate qualification.
PR #69 subsequently merged those changes to `dev` at
`49fa97ee3a709be122298ad5dbaa3fa693c15b72` on 2026-09-12T16:55:33Z. The
candidate synchronization commit `c9a4023087fc403e3aa593570b391b1612ed6141`
joins the prior local L16 handoff with that exact `origin/dev` merge. This
ancestry fact does not replace candidate validation or authorize a deployment.

| Command or check | Environment | Actual result | Evidence and limitation |
| --- | --- | --- | --- |
| Brand provenance inventory test | Vitest unit project | Passed, 1 file / 3 cases | The existing registry-asset test now also binds the five project-owned SVG paths, byte lengths, and SHA-256 values to `apps/www/public/brand/README.md` and its root-MIT link. It does not duplicate registry-license propagation coverage. |
| Browser raster review of the actual SVG URLs | Playwright Chromium 1.61.1 on Windows | Passed | The favicon and standalone marks remained recognizable at 16 px and 32 px on light and dark backgrounds. The blue fold occupied 15 pixels at 16 px and 55 pixels at 32 px; foreground bounds remained within pixels 1–14 and 2–29 respectively. The 150 × 32 and 300 × 64 lockups remained legible. Black/white contrast ratios were 19.80 and 18.97; blue contrast was 5.17 on white and 3.83 on near-black. The temporary contact sheet was deliberately not added to tracked evidence. |
| One-off local navigation check against the static build | Playwright Chromium with the same-origin Vercel script intercepted by a deterministic recorder | Passed | The injected script declared `sdkn=@vercel/analytics/next` and `data-disable-auto-track=1`. It emitted exactly one initial pageview for `/`, one after client navigation to `/docs/`, and one after navigation to `/templates/`, with no page error. This checks adapter integration and duplicate suppression, not Vercel backend ingestion. |
| `corepack pnpm@11.24.0 build` | Windows, Node 24.18.0, pnpm 11.24.0 | Passed as a preflight only | Generated 18 template PDFs, 21 preview pages, 62 registry items, 28 component PDFs, three theme PDFs, and 49 static routes. S01 documentation changed concurrently and was committed afterward, so the recorded workspace fingerprint names `6207e72`; it is intentionally not accepted as current-candidate evidence. |
| Existing-build bundle measurement with Node `node:zlib` | Same local build | Measured; no threshold failure claimed | The layout chunk containing the Next adapter measured 7,690 raw bytes / 3,111 gzip bytes, versus the earlier React-adapter evidence of 6,376 / 2,715: +1,314 raw / +396 gzip. Fourteen initial scripts totaled 778,947 raw / 245,832 gzip. The previously documented `pnpm verify:bundle` command did not exist; `docs/TESTING.md` now documents the real fingerprint-plus-recorded-measurement procedure instead of inventing a passing verifier. |

The four lockup files use live text with a `Geist, Inter, Arial, sans-serif`
fallback stack. The current browser review passed, but exact glyph metrics can
vary outside the site because the standalone SVGs do not embed a font. This is
an explicit asset limitation, not a cross-platform visual guarantee.

## Exact local v1.0.0 candidate qualification

The first locally qualified candidate was
`7ccdd10c25d08fa929d805a3c1b09ada9e0aba65`. After PR #69 merged to `dev`,
the branch synchronized at
`c9a4023087fc403e3aa593570b391b1612ed6141` and the complete invalidated
qualification was rerun successfully. The current local L16 sequence is:

1. `486b3201591f41f34c39d5214ef923693a1d63a1` — release documentation,
   licenses, and release notes.
2. `fe13298a17d4e17fbd80041efedc7563f358b26b` — brand-asset provenance
   and measured bundle evidence.
3. `0e38d30b2e8b99ab40a5d138d128da0fe5e92201` — v1.0.0 package,
   catalog, registry, metadata, immutable-path, consumer, and preview contracts.
4. `3fea729b8e81a8da8f9e1f70f6fd130bc8f350d0` — pinned nested pnpm
   invocations discovered during qualification.
5. `7ccdd10c25d08fa929d805a3c1b09ada9e0aba65` — shared TypeScript API
   reader setup discovered during qualification.
6. `d40273cbb7d8ea49243931a694742475ff6e6e30` — first local candidate
   evidence and handoff.
7. `c9a4023087fc403e3aa593570b391b1612ed6141` — synchronization with the
   merged PR #69 `origin/dev` base.

The package metadata is `1.0.0`, and the generated registry is `v1.0.0`.
Release dependencies and installation commands remain on that same immutable
version. The mutable development version still exists as an explicit local
development mode; it is not referenced by the candidate's public install
surface.

### Candidate verification

| Command or scope | Actual result | Evidence |
| --- | --- | --- |
| `corepack pnpm@11.24.0 validate:full` | Passed at `c9a4023087fc` | Quality, build, and all active test scopes completed sequentially against the exact synchronized candidate. The observed output was not persisted as a log artifact; the counts below are transcribed from that completed run. |
| Formatting, lint, and TypeScript | Passed | Prettier check, zero-warning ESLint, workspace TypeScript, application type generation, and root TypeScript all completed. |
| Static build and generators | Passed, 49 routes/pages | Generated 18 template PDFs / 21 preview pages, 62 `v1.0.0` registry items, 5 verified assets, 28 component PDFs, and 3 theme PDFs. |
| Unit and component projects | Passed, 21 files / 73 tests | Each lightweight project was collected once through the aggregate runner. |
| Actual-PDF project | Passed, 5 files / 12 tests | Existing geometry, content, pagination, barcode, graph, and final-PDF boundaries remained qualified. |
| External consumers | Passed, 1 file / 1 test in 353.61 seconds | Browser and Node consumers installed from the local `/r/v1.0.0/` registry, prepared 5 verified assets, compiled, built, and rendered after the registry server stopped. |
| Playwright E2E | Passed, 3 tests | Reused the candidate fingerprinted static build. |
| Exact visual reference | Passed, 1 file / 1 test | Existing selected family references remained unchanged. |
| Source-document links | Passed | Verified 44 public pages and 2,562 local links including anchors. |
| `node tooling/testing/build-fingerprint.mjs verify` | Passed | Verified the recorded schema-5 fingerprint against synchronized candidate `c9a4023087fc`. |
| Dedicated `corepack pnpm@11.24.0 build:preview` followed by `corepack pnpm@11.24.0 preview:verify` | Passed | Rebuilt the local non-indexed release preview and verified the v1 registry origin, deep page, PDF, JSON, font, JavaScript, MIME, CSP, cache policy, and `robots.txt` over loopback HTTP. |

The synchronized candidate fingerprint records 254 source inputs with input SHA-256
`f184499a192e7c10dc74a0c87f0a3e22f571f62b92bac3bdaccc784dc125ba90`.
The static output contains 465 files / 40,002,524 bytes with output SHA-256
`36fb2d5ec597438949b1b333a9816bdeb884f717128d72a3f5e368a726a7ee94`.
Its configuration is `SITE_URL=http://127.0.0.1:4173`, registry origin
`http://127.0.0.1:4173/r/v1.0.0/`, registry version `v1.0.0`, and indexing
disabled. These loopback values prove a reproducible local preview only; they
are not production URLs or deployment evidence.

## Preserved public-beta evidence

The beta at `https://docn-ui.vercel.app` was previously built, verified on an
isolated Vercel deployment, promoted to the stable alias, and exercised through
one external shadcn installation from `/r/dev/`. The licensed beta build used
source merge `2b589154686f75b27b96eac2d0014b72d21ac158`, input digest
`f5080ee51410588ff5c5967a87866920909239ffdee20e0332900240d4184954`,
output digest
`2ec184b2d4fa62504d72f9558925ed6f068ac5b3b46b81524cf2c8d9e7ef0efd`,
and deployment `dpl_C2cnwaVHtVgDUrDViti9FPBWJxza`. That evidence qualifies the
public beta only; it is not reusable as an official v1.0.0 candidate
fingerprint.

PRs #54–#58 each passed the seven required GitHub Actions checks applicable to
`dev`: branch policy, quality, unit, build, PDF, E2E, and consumers. The
subsequent promotions also produced successful `release-policy` checks, but the
active `protect-main` ruleset does not yet require that context. Adding it is a
separate authorized governance action before the official promotion.

## Local candidate handoff and remaining release gates

Local preparation and proportional qualification are complete at `c9a4023`.
The safe resumption point is the unchanged `codex/l16-v1-candidate` branch and
the fingerprint above. The existing maintenance and rollback procedure remains
in `docs/RELEASE.md`: never overwrite a published version, retain the complete
fingerprinted artifact, and send any corrective documentation or code back
through `dev` under normal review.

Delivery remains incomplete for all of the following independent reasons:

- PR #69 is merged to `dev`, and the synchronized candidate contains that exact
  `origin/dev` merge as an ancestor. However, no release PR exists, and neither
  the candidate branch nor its exact artifact has entered protected-branch CI
  or review.
- Official v1.0.0 release authorization has not been granted. Earlier beta and
  interim-promotion approvals do not authorize this candidate.
- No candidate preparation merge to `dev`, exact `dev -> main` promotion,
  production deployment, annotated tag, or GitHub Release exists.
- The active `protect-main` ruleset still does not require the already-observed
  `release-policy` context.
- `/r/v1.0.0/` has been verified only on the local candidate preview. No public
  v1 origin, deep-link/download smoke check, version-ID installation, deployment
  fingerprint, production error-log review, or rollback observation exists.
- Issue #20 and its Project item must remain open and In progress. They may move
  to Done only after authorized, verified public delivery.

The next permissible action is maintainer review of this synchronized local
candidate and a separate decision to create its remote preparation PR. Remote
branch creation, PRs, ruleset changes, labels, merges, deployment, tagging, and
release creation remain separate actions; this handoff authorizes none of them.

## Deviations and resumption

Earlier L16 evidence said that no `dev -> main` promotion existed. That became
stale after PRs #60, #62, and #64 and is corrected here without reclassifying
those interim promotions as the official release. The L16 tracking record also
omitted PRs #56–#58; S01 records those observed merges and keeps the issue open.

`docs/implementation/status.json` records `verified_local`, the exact
synchronized candidate fingerprint, all preceding actual L16 commits including
the first handoff and synchronization merge, and the concrete remote blockers.
This is not `in_review`, `merged`, `released`, or G6 delivery.
The existing `publicationAuthorized` value remains narrowed by explicit beta
scope and a separate false official-v1 authorization field. This records
missing authority, not a maintainer refusal.

The first S01 link check could not run without ignored static output. It passed
after the exact candidate build produced that prerequisite, so no documentation
link limitation remains locally. The two qualification fixes were committed
separately and the complete invalidated validation was first rerun at their
descendant `7ccdd10`, then rerun again after synchronization at `c9a4023`; no
failing intermediate commit is presented as verified.

No remote branch, PR, merge, ruleset, deployment, tag, GitHub Release, issue,
or Project state changed during this local handoff. No public delivery is
claimed.
