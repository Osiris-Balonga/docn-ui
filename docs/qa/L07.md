# QA — L07 — registry and consumption outside the monorepo

Date: 2026-08-30. Status: **in progress**. Branch `feat/shadcn-pdf-registry`, base `4257ac9bdba714028a717aa2be2c56b2689bc3e2`. Issue [#10](https://github.com/Osiris-Balonga/docn-ui/issues/10), Project item `PVTI_lAHOCZDjCM4BhyZuzg4fVcs` in `In progress`. No PR or merge commit exists yet.

## Scope

L07 will generate versioned shadcn registry items from the existing document sources, distribute verified local assets, expose complete source and installation instructions, and prove installation and rendering in isolated browser and Node consumers.

## Verification

| Command or check | Environment | Actual result | Evidence |
| --- | --- | --- | --- |
| L06 merge and L07 base observation | GitHub PR/API and local Git | Passed | PR #26 merged at `2026-08-30T07:32:47Z`; local `dev` fast-forwarded to merge `4257ac9bdba714028a717aa2be2c56b2689bc3e2` before creating the L07 branch |

## Justification for added tests

Planned coverage is limited to registry graph/import transformations, shared asset security boundaries, one source/copy composition, and two distinct isolated consumers. Static verification will cover every item; the expensive real CLI path will install one business-card dependency graph rather than all three cards.

## Visual and document checks

No L07 document output has been qualified yet. Consumer rendering will reuse the existing card geometry/content assertions and inspect one actual installed PDF; it will not create a second template matrix.

## Deviations and resumption

L06 PR #26 passed all six required checks and merged into `dev` with merge commit `4257ac9bdba714028a717aa2be2c56b2689bc3e2`; issue #8 closed and its Project item moved to `Done`. L07 starts exactly from that observed merge. Resume at L07-S01.
