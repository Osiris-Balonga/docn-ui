# QA — L05 — business cards from editing to export

Date: 2026-08-29. Status: **in progress**. Branch `feat/business-card-templates`, base `37d799556498c7019ac53ebfcf74816031c011de`. Issue [#7](https://github.com/Osiris-Balonga/docn-ui/issues/7), Project item `PVTI_lAHOCZDjCM4BhyZuzg4fVYI` in `In progress`. No PR yet.

## Scope

L05 implements the first complete product workflow: three structurally distinct, two-sided business-card templates, validated editing, actual browser-worker preview, and faithful download. It reaches G2 without introducing the general catalog, registry distribution, or a second document implementation.

## Verification

| Command or check | Environment | Actual result | Evidence |
| --- | --- | --- | --- |
| `corepack pnpm test:pdf business-card` | Windows, Node 24.18.0, pnpm 11.24.0, React-pdf 4.9.0, PDF.js 6.2.108 | Passed: one family file, two focused cases | Minimal nominal render plus name/address fixed-layout overflow |
| `corepack pnpm --filter @docn-ui/documents typecheck` | Same | Passed | Strict schema, template, metadata, plan, and package exports |
| Targeted ESLint for the changed primitives and business-card sources | Same | Passed with zero warnings | Changed L05-S01 source and PDF family test only |

## Justification for added tests

Extend one business-card PDF family suite for actual dimensions, sides, content, and fixed-layout overflow. Add one focused form component suite for validation and renderer coordination, then one Playwright Chromium journey because browser worker preview and downloaded bytes cannot be proved by unit or component tests. Do not copy the journey for each composition or multiply theme/format combinations.

## Visual and document checks

The minimal French fixture produced a 10,142-byte, two-page PDF at exactly 240.945 × 155.906 pt per page. PDF.js extracted the identity, all four contact details, and the back-side brand while every measured text bound remained inside the 3 mm safe frame. The first 144 DPI rasterization exposed an e-mail/website collision between two columns; the layout was changed to aligned full-width rows, regenerated, and inspected with no remaining overlap or clipping. The back uses the controlled inverted theme color. PNG intermediates were removed; the PDF remains an ignored local artifact. Contact-sheet, browser preview/export, and physical-print evidence remain pending.

## Deviations and resumption

L04 PR #24 was merged into `dev` at `2026-08-29T21:46:15Z` with merge commit `37d799556498c7019ac53ebfcf74816031c011de`; issue #6 closed and its Project item moved to `Done`. L05 starts from that observed merge. The shell exposed pnpm 11.19.0 even though the project pins 11.24.0; Corepack resolved and ran the declared version without a dependency or lockfile change. Resume at L05-S02 after recording the L05-S01 commit SHA.
