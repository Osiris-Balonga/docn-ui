# QA - L04 - document foundations

Date: 2026-08-29. Status: **in progress**. Branch `feat/document-foundations`, current recorded SHA `fda84780c856b813555cbc617c694101562347a9`, base `55deb07d24f5a8d4bfa0b7a03d71f5cadcc8f888`. Issue [#6](https://github.com/Osiris-Balonga/docn-ui/issues/6), Project item `PVTI_lAHOCZDjCM4BhyZuzg4fVV8`. No PR yet.

## Entry and scope

PR #23 merged into `dev` at `2026-08-29T07:07:14Z`, merge `55deb07d24f5a8d4bfa0b7a03d71f5cadcc8f888`. Issue #5 closed and Project L03 moved to Done; L04 moved to In progress. No main promotion, deployment, package publication, or license selection occurred.

L04 promotes the qualified PDF spike into small reusable contracts, formats, PDF-only themes, controlled assets, and the layout primitives needed by the first business card. It does not add public templates, a universal layout engine, or unrelated invoice/table abstractions.

## Verification

| Command or check | Environment | Actual result | Evidence |
| --- | --- | --- | --- |
| `pnpm test:unit formats` | Windows, Node 24.18.0, pnpm 11.24.0 | Passed: one file, four focused cases | Format compatibility, orientation, custom bounds, normalization and limits |
| `pnpm test:unit themes` | Same | Passed: one file, two focused cases | Portable tokens, bounded scale, controlled browser/Node sources, file sizes and SHA-256 hashes |
| `pnpm test:unit measurement` | Same | Passed: one file, three focused cases | Exact safe frame, structured overflow, and site/DOM-CSS import boundary |
| `pnpm test:pdf feasibility` | Same, React-pdf 4.9.0, PDF.js 6.2.108 | Passed: existing one file, six cases | Neutral/editorial card, shared primitives with actual glyph bounds, retained geometry, boxes, pagination, and measured receipts |
| `pnpm --filter @docn-ui/documents typecheck` | Same | Passed | Strict package contracts and public exports |
| Targeted ESLint for changed document, asset, theme, and copy-tool files | Same | Passed with zero warnings | Changed L04-S01/S02 files only |
| `pnpm build` | Same, Next.js 16.3.3 webpack static export | Passed | Five static routes; worker chunk references the manifest; all four local fonts copied to output |

## Justification for added tests

S01 adds one unit file because the unit scope previously had no document-core suite. Its four cases cover distinct pre-render risks together: applying orientation once, rejecting template/format incompatibility with field paths, enforcing custom versus preset dimensions, and preserving invalid data while reporting limits. S02 adds one theme/asset file because token validation and path/hash controls are separate from format behavior. Its two cases group the three themes and four assets instead of creating a theme × weight × runtime matrix. S03 adds one measurement file with three separate low-level risks rather than component tests per primitive. Neither duplicates L02's real-PDF geometry assertions; the existing feasibility file contains the one real primitives composition.

## Visual and document checks

No PDF was generated for S01 because contracts and format resolution are pure functions, while L02 already proves the mm-to-point geometry with actual files. S02 extended that existing fixture with an editorial render using local Noto Serif headings and Noto Sans body text. PDF.js extracted French accents from both 85×55 mm pages. Poppler reported two 240.945×155.906 pt pages and a 9,751-byte PDF; both pages were rasterized at 144 DPI and inspected. The first render exposed a title/accent overlap on the back; using the bounded heading token there removed it, and the latest rasterization showed legible text, intact accents, consistent margins, and no overlap or clipping.

S03 rendered one 7,924-byte, one-page primitives card at the same exact size. PDF.js glyph positions were converted to top-left bounds and every measured text item stayed within the 3 mm safe frame. Its 144 DPI rasterization showed the Serif heading, bilingual supporting text, separator, and two field pairs with no clipping or overlap. PNG intermediates were removed and no visual snapshot matrix was added. Physical printing remains outside this lot.

## Deviations and resumption

Zod 4.5.2 was initially considered from the live npm metadata, but it had been published on the same day and caused pnpm to propose an age-policy exception. The exception was removed and the already locked MIT-licensed Zod 3.25.76 was promoted to a direct dependency instead, avoiding a second copy and preserving the existing supply-chain policy. The Noto Serif package tarball also stalled; exact versioned files were obtained from one package CDN and matched against a second mirror, with hashes recorded in the manifest. No runtime CDN request exists.

`pnpm test:pdf primitives` was tried once and correctly failed because the native positional filter matches file paths, while the primitives case intentionally remains in `feasibility.pdf.test.ts`. The targeted rerun used `pnpm test:pdf feasibility -t "composes shared primitives"`, then the complete `pnpm test:pdf feasibility` file passed with six cases. Resume at L04-S04 on `feat/document-foundations`.
