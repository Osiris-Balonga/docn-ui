# QA — L12 — reusable components and documentation

Date: 2026-08-31. Status: **in progress**. Branch: `feat/documentation-catalog`, based on L11 merge `a342433e0902935a454d8ef04a85cb508a765f4a`.

Issue [#16](https://github.com/Osiris-Balonga/docn-ui/issues/16); Project item `PVTI_lAHOCZDjCM4BhyZuzg4fVmY`. The maintainer explicitly authorized L11's merge and L12 startup. No L12 merge or release is authorized by that decision.

## Scope

L12-S01 and S02a–S02g are implemented and locally verified. Eight guides cover installation, local assets, independent browser/Node usage, themes, formats, locale/data, updates and limitations. Shared frames, core content, pagination/tables, printable forms/annotations, vector graphs and validated barcodes are implemented below. The documentation index, sidebar, search and section anchors expose the same guide inventory. Template redesign remains paused. The remaining component story is S02h; the lot is not yet verified or ready to merge.

The browser, Node and Vite build snippets come from `packages/documents/src/examples/consumer-usage.ts`, also consumed by the existing external installation scenario. Documentation and template source share token rendering, code scrolling and accessible copy controls. Shell commands deliberately avoid TypeScript tokenization. Installed source uses the configured registry origin, with an explicit distinction between the default registry preview on port 4173 and a documentation server on port 3000.

## Verification

Entry tracking commit: `901907af6a08f40d32e40a7f0dc7b63cac1c3e52`. S01 implementation and tests: `6c8563b8a10de329d0aac10a85f9d509699dffbc`. Results below were executed on the S01 working tree before its commit; no claim of a remote L12 CI run or completed lot is made.

| Command or check | Environment | Actual result | Evidence |
| --- | --- | --- | --- |
| L11 merge prerequisite | GitHub | Passed | Seven successful checks at `98cab3d`, observed merge `a342433` and explicit authorization. |
| `pnpm quality` | Windows, Node 24.18.0, pnpm 11.24.0 | Passed | Code/config formatting, lint and workspace/root types. Targeted formatting/lint and the build's types also passed after the final mobile text-wrap adjustment. |
| `pnpm test:components shell-navigation registry-source-panel` | jsdom | Passed, 2 files / 5 cases | Existing search/navigation and source/copy coverage; no duplicate component suite. |
| `pnpm test:consumers` | Two temporary projects outside the monorepo | Passed, 1 orchestral case | Official shadcn installs; the shared browser/card and Node/invoice snippets build and produce actual PDFs after the test registry is stopped. |
| `pnpm build` | Static Next.js export | Passed, 30 generated pages | Eight new guide paths; registry/assets generated and build fingerprint recorded. No PDF layout changed. |
| `pnpm verify:docs` | Existing static output, JSDOM without script execution | Passed, 10 documentation pages / 254 local links | Includes fragment targets and one main heading per page; also runs after the existing CI build. |
| `E2E_USE_BUILD=1 pnpm exec playwright test tests/e2e/catalog.spec.ts --grep 'documentation navigation'` | Chromium, existing static build | Passed, 1 case, 4.6 seconds | Desktop guide discovery, keyboard code scrolling, hidden code scrollbars, single mobile menu, guide navigation and no horizontal body overflow at 375 px with reduced motion. |
| `git diff --check` | Local | Passed | No whitespace errors. |

## Justification for added tests

The link checker detects missing generated guide routes and broken section anchors without another browser journey. The existing navigation journey now covers discovering a guide, keyboard scrolling in shared code blocks and mobile guide navigation. The existing consumer test consumes the exact documentation snippets, preventing installation examples from drifting. No new PDF fixture, theme matrix, full PDF suite or physical printing test was added for this documentation change.

## Visual and document checks

Inspected the dark desktop Browser and Node guide in the in-app browser: existing header/sidebar/TOC alignment, quiet shared code surfaces and no visible code scrollbars. Inspected the actual light mobile Local assets screenshot at 375 × 812. The paragraph's long resolver name wraps inside the viewport; code retains independent horizontal scrolling. The E2E journey also checks navigation at 1015 and 700 px.

Local screenshot: `.artifacts/e2e/test-results/catalog-keeps-the-Home-foc-7c89b-only-when-space-requires-it/documentation-mobile.png` (generated evidence, not committed). No fabricated image, new PDF layout or printing claim is part of S01. Reduced-motion preference was enabled for the mobile check; 200% browser zoom and physical printing were not tested.

## Deviations and resumption

- Typed local content replaces the proposed MDX content format for S01. Both retain a trusted, versioned source boundary; this avoids adding a parser dependency for eight structured guides. Architecture and lot acceptance text record the choice.
- Initial lint found an unused import after extracting the copy control; removed before the successful quality run. An initial navigation selector matched both sidebar and content links; scoped it to the guide index region. The 375 px check then exposed real paragraph overflow from a long function name; added article-level wrapping and reran successfully. No threshold was weakened and no failing commit was created.
- No new dependency, license change, publication, deployment, template redesign or automatic theme-inheritance promise.
- S01 resumption continued into S02a below. Keep the lot in progress until all ten planned stories and their evidence are complete; a L12 merge still requires explicit authorization after full verification.

## S02a — shared themes and fixed/flow frames

Implementation, tests and evidence commit: `06697e17ae68751df08bd4d880b82c7389399711`. Checks below were executed on the story working tree before its commit; no remote CI or completed-lot claim is made.

Implemented after the maintainer accepted S01. `PdfThemeProvider`/`usePdfTheme` no longer require fixed geometry. Existing primitives are split into theme, typography, layout, image, QR and fixed-frame modules, with an unchanged compatibility facade. Pure print geometry is extracted without changing its calculations. No existing template source or design is modified.

`DocumentFrame` adds normal-flow portrait A4/Letter pages with explicitly reserved repeated header/footer regions, independent flow geometry access and measured non-breaking-block validation. The exact API was documented in COMPONENT_CATALOG before its dependents were implemented. It supports trim-sized screen geometry initially; arbitrary JSX height, bleed profiles and automatic oversized-content splitting are not implied. S02c will build reusable pagination components on these bounds.

### Executed checks

| Check | Actual result | Evidence |
| --- | --- | --- |
| Existing nominal fixtures before extraction | Passed, 6 existing cases | Three cards, three tickets plus long-title case, three receipts, three labels and three invoices: 16 baseline PDFs retained. |
| `pnpm test:unit measurement themes` | Passed, 2 files / 8 cases | Safe dimensions, region reservations, bad sizes/formats, oversized block errors, explicit theme access and existing asset checks. |
| `DOCN_WRITE_PDF_ARTIFACTS=1 pnpm test:pdf` | Passed, 7 files / 20 cases | All existing family/feasibility tests plus one shared fixed/flow specimen. Includes print boxes, QR decoding, continuous receipts, imposition and multipage invoice coverage affected by the shared module extraction. |
| Poppler before/after raster comparison | Identical, 16 PDFs / 19 pages | All rendered PNG SHA-256 values match at 72 dpi. Baseline references were copied before code changes, not regenerated to hide a difference. |
| New specimen visual inspection | Passed, all 3 pages | Actual Poppler images at 85 dpi: shared fixed/flow typography, scoped nested serif headings, repeated header/footer, no overlap or clipping, 48 entries and final content preserved. |
| `pnpm quality` | Passed | Formatting, lint and workspace/root types. React review kept new imports direct, providers geometry-specific, and PDF JSX free of site dependencies. |
| `pnpm test:unit registry` / `pnpm verify:registry` | Passed, 3 cases / 24 registry items / 5 assets | New module paths are part of the existing aggregate item; no new dependency or individual component availability claim. |
| `pnpm test:consumers` | Passed, 1 orchestral case, 31.04 seconds | Browser card and Node invoice install through the official CLI, retain consumer configuration, build and render after the registry is stopped. Source closure counts are 42 and 43. |
| `pnpm build` / `pnpm verify:docs` | Passed, 30 pages / 10 docs pages / 254 links | Static build and existing docs remain intact. No new site page or UI behavior in S02a. |

### Evidence, corrections and limits

Generated files (not committed): `.artifacts/l12/baseline-s02a`, `raster-before`, `raster-after`, `pdf/primitive-frames.pdf`, and `rendered/primitive-frames-{1,2,3}.png`. The PDF test asserts exact A4 dimensions, three physical pages, each entry once, body bounds excluding repeated regions, final content and nested-theme isolation. This is one reusable specimen, not a template/theme matrix.

During development, the nested-theme assertion initially used body text, but both themes intentionally share Noto Sans body fonts; it now tests their different heading fonts. A missing-provider check through the PDF reconciler exposed the engine's secondary error wrapping; that guard is tested at the unit boundary with a controlled context value, while the real specimen verifies actual provider nesting. A server-DOM test helper was unavailable in the PDF workspace and was not added as a dependency. The external-install test initially expected the previous 32/33 source files; it now explicitly expects 42/43 (one extracted core geometry file and nine primitive modules). The actual external render still runs; no check or threshold was disabled.

No new dependencies, no physical printing, no template restyling, no public release and no claim of a finished component catalog. No additional responsive/keyboard test was added for a PDF-only source change; S01 covers the unchanged site shell. S02a continued into S02b below. L12 remains **in progress**.

## S02b — composable typography, layout and media

Implementation, tests and evidence commit: `0a9aeebaa2360f658cbd870e40187ab70c5dfac2`. Checks below were executed on the story working tree before its commit; no remote CI or completed-lot claim is made.

Implemented after the maintainer approved continuation from S02a. The content API decision was recorded in COMPONENT_CATALOG before implementation. Heading now supports numeric hierarchy/alignment/destinations; Text supports trusted inline content and explicit weights/alignment/destinations. Stack and Row share directional layout behavior. Divider/Separator and KeyValue/FieldPair are exact aliases, with horizontal metadata available in addition to the legacy vertical layout.

Section, Card, Link and List add neutral themed grouping, native validated PDF annotations and bounded nested content (100 total items, depth 3, 2,000-character fields). List check states are vector print marks, not interactive controls. Image adds controlled fit/alignment/caption, positive physical dimensions and a local-only source envelope. Pre-decoding, the 16-megapixel check and blob lifetime stay at the existing asset boundary; an opaque blob URL does not prove its own byte size. No remote-image fetch, unregistered font/style option or visitor JSX evaluation is added.

Existing default API output remains unchanged. The QR encoder, four-module quiet zone and density logic are untouched. Six new runtime modules enter the existing aggregate registry item; individual items and clickable gallery/detail pages remain S02g/S02h, not completed by this story.

### Executed checks

| Check | Actual result | Evidence |
| --- | --- | --- |
| `pnpm test:unit content.test measurement themes` | Passed, 3 files / 12 cases | Four focused content cases cover identity aliases, valid/unsafe links and IDs, list limits/cycles, local source envelopes and image dimensions; existing geometry/theme checks reused. |
| `DOCN_WRITE_PDF_ARTIFACTS=1 pnpm test:pdf primitives` | Passed, 1 file / 2 cases | Existing fixed/flow specimen plus one new combined A4 content specimen. |
| `DOCN_WRITE_PDF_ARTIFACTS=1 pnpm test:pdf` | Passed, 7 files / 21 cases | One regression run because shared Text/Row/Image behavior affects the template families. Existing QR decoding cases reused; no new QR matrix. |
| Poppler raster comparison at 72 dpi | Identical, 16 PDFs / 19 pages | New family PDF output compared against the unchanged S02a baseline rasters by SHA-256. No reference regenerated. |
| Combined content PDF inspection | Passed, 1 A4 page | Actual Poppler render at 100 dpi inspected: monochrome hierarchy, metadata, indented nested list, numbered/static checked list, local image/caption, QR and navigation; no text clipping or overlap observed. |
| `pnpm quality` | Passed | Formatting, lint and workspace/root types; new PDF code remains independent of site components and uses direct module imports. |
| `pnpm test:unit registry` / `pnpm verify:registry` | Passed, 3 cases / 24 items / 5 assets | Aggregate source closure contains the six added modules, no new dependency. |
| `pnpm test:consumers` | Passed, 1 orchestral case, 29.58 seconds | Official shadcn installation into two temporary external projects, retained configuration, builds and actual browser/Node PDF renders after stopping the registry. Exact aggregate counts updated from 42/43 to 48/49 with the six-module explanation. |
| `pnpm build` / `pnpm verify:docs` | Passed, 30 pages / 10 docs pages / 254 links | Unchanged catalog/docs routes build against the new PDF source. |
| `git diff --check` | Passed | No whitespace errors. |

### Evidence and limits

The reusable trusted example is `packages/documents/src/examples/primitive-content.tsx`; it accepts an already validated local image rather than importing test-only image tooling. The existing PDF test file supplies a small generated monochrome PNG, renders the actual document and independently inspects it with PDF.js. Assertions cover page count, selectable accented text, different regular/strong fonts, physical content bounds, external HTTP/mail/tel annotations with nonzero rectangles, a resolvable internal destination, and an embedded image paint operation.

Generated QA files (not committed): `.artifacts/l12/pdf/primitive-content.pdf`, `.artifacts/l12/rendered/primitive-content.png`, and `.artifacts/l12/raster-s02b`. The existing `baseline-s02a` and `raster-before` references are unchanged. The four-page total across the old and new specimens is not a theme/locale matrix.

A first formatting command used an unquoted brace glob that PowerShell rejected before running; quoting the glob fixed the invocation. All executed behavior checks passed; no failing commit, weakened threshold or replacement visual reference was used. No dependencies, license changes, template redesign, physical printing, PDF/UA claim, deployment or public release. No new responsive/keyboard test for this PDF-only source change; the unchanged site shell retains S01 evidence.

S02b continued into S02c below. L12 remains **in progress**, not `verified_local`, merged or released.

## S02c — reusable pagination and tables

Implementation, tests and evidence commit: `e824244fd6034afd493fdfac2954cf49cdbd9441`. Checks below were executed on the story working tree before its commit; no remote CI or completed-lot claim is made.

Implemented after the maintainer approved continuation from S02b. The pagination/table API was recorded in COMPONENT_CATALOG before implementation. KeepTogether and PageBreak use the existing flow frame; PageHeader/PageFooter compose either inline or inside its reserved repeated regions. PageNumber uses the engine's final-pagination callback and a bounded configurable format.

Table/Row/Cell/Header and DataTable share the existing invoice FlowTable helpers. Their additions are optional helper props: inline header positioning, declared minimum height and header text styling. Existing invoice source and default helper calls remain unchanged. Public table columns use validated percentage widths and unique keys; typed data accessors produce bounded strings/finite numbers and unique row keys. Rows and non-breaking groups require explicit premeasured heights and reject declarations exceeding the body or excluding required cell padding. No second table layout engine or automatic JSX-height estimator was added.

Five source modules join the existing aggregate registry. Individual installation entries and public component detail pages remain S02g/S02h.

### Executed checks

| Check | Actual result | Evidence |
| --- | --- | --- |
| `pnpm test:unit table.test measurement registry` | Passed, 3 files / 12 cases | Four new focused cases cover column structure/widths, typed rows/keys/value limits, impossible row heights and page-number formats. Existing geometry and registry coverage reused. |
| `DOCN_WRITE_PDF_ARTIFACTS=1 pnpm test:pdf primitives invoice` | Passed, 2 files / 6 cases | Existing shared frame/content specimens, one new pagination specimen, nominal invoices and both existing long-invoice cases. No unrelated family suite or QR matrix rerun. |
| Poppler before/after invoice comparison at 72 dpi | Identical, 4 PDFs / 7 pages | Three nominal invoices plus the real long invoice. Previously verified S02b PDF artifacts were copied before regeneration; all corresponding PNG SHA-256 hashes match. |
| Multipage specimen visual inspection at 90 dpi | Passed, all 4 pages | Forty entries over two pages, a complete summary on page 3 and explicit appendix break on page 4. Repeated title/column headers, contact footer and final page numbering are visible with no content overlap/clipping. |
| `pnpm quality` | Passed | Formatting, lint and workspace/root types. New imports remain module-specific and all PDF composition stays independent of site UI. |
| `pnpm verify:registry` | Passed, 24 items / 5 assets | Updated aggregate closure verifies against the pinned schema; no new dependency. |
| `pnpm test:consumers` | Passed, 1 orchestral case, 162.63 seconds | Official CLI installation in two temporary external projects with unchanged configuration; actual browser/Node renders after the registry stops. Counts are 53/54, explicitly accounting for five added modules. |
| `pnpm build` / `pnpm verify:docs` | Passed, 30 pages / 10 docs pages / 254 links | Existing site and guides compile with the expanded source closure. |
| `git diff --check` | Passed | No whitespace errors. |

### Evidence, correction and limits

The reusable example is `packages/documents/src/examples/primitive-pagination.tsx`. The existing primitive PDF suite independently verifies four physical pages; every entry 001–040 exactly once; the complete wrapped first description; the final amount and summary marker together on page 3, not on page 2; the manual Table composition and empty DataTable on page 4; repeated headers/contact details; and exact final numbering on every page. Text coordinates are checked against separate header/body/footer rectangles, not only file size or text presence.

Generated artifacts (not committed): `.artifacts/l12/pdf/primitive-pagination.pdf`, `.artifacts/l12/rendered/primitive-pagination-{1,2,3,4}.png`, `baseline-s02c`, `invoice-before-s02c` and `invoice-after-s02c`. The existing consumer artifacts/log were regenerated by the successful scenario.

The first real PDF test exposed missing PageNumber text. Reserving width/minimum height did not fix it. Temporary inspection through the engine's onRender layout data showed a final numeric lineHeight of 3241.35 for 7 pt text and an empty laid-out line list: renderer 4.9.0 repeatedly resolves numeric lineHeight during dynamic pagination. PageNumber now leaves line height to font metrics, following the working native invoice callback pattern; its footer gets a bounded column. The final PDF test verifies all four numbers without weakening its assertions. Temporary diagnostics and the ineffective minimum-height workaround were removed. Initial lint also requested an explicit Image alt prop despite the typed spread; the header now supplies it explicitly. No failing commit or upstream engine patch was created.

Heights remain an explicit author/premeasurement contract: callers must measure cells at the actual full body width and selected font, including padding/borders, and reserve enough header/footer space. Arbitrary nested cell JSX, automatic oversized-content splitting, clipped/shrunken rows and implicit font/layout measurement are not promised. Repetition belongs to DocumentFrame; separate frames define different repeated headings. The initial flow-page format remains portrait A4/Letter screen/trim geometry. These are source capabilities, not yet public gallery completeness.

No dependency, physical printing, template restyling, PDF/UA claim, deployment, release or L12 merge. Responsive/keyboard checks were not repeated for this PDF-only change; S01 covers the unchanged site shell. Next story: **L12-S02d**, printable form and annotation components. L12 remains **in progress**.

## S02d — printable forms and annotations

Implementation, tests and evidence commit: `4c4a9d75197eb670fbd244b459e8af8c5e638005`, based on `3cf96d4343c8a570e3340834f03902f00d9c83b3`. Checks were executed on the story working tree before that commit. No remote CI or completed-lot claim is made.

Implemented after the maintainer requested continuation from S02c. The printable API decision was written in COMPONENT_CATALOG before implementation. Alert and Badge expose readable status labels with neutral theme roles. Form validates bounded, uniquely identified groups/fields and composes one/two/three-column printable rows, including blank values, visible required labels and multiline values. Signature offers single/dual areas and a single inline field with bounded writing space and optional name/role/date. Watermark uses the DocumentFrame body rectangle, bounded opacity/font size, conservative Latin-text envelopes and top/center/bottom placement; final subpage numbering controls first-page-only marks independently of repetition.

No DOM controls, AcroForm widgets, cryptographic signatures, signing services or document-protection features are introduced. Six runtime modules join the existing aggregate registry; no dependency was added. Existing templates, base primitive output and the site shell are unchanged. Individual registry entries and public component detail pages remain S02g/S02h.

### Executed checks

| Check | Actual result | Evidence |
| --- | --- | --- |
| `pnpm test:unit content.test measurement registry` | Passed, 3 files / 15 cases | Existing suite extended with three focused cases for field/group limits and identity, signer layouts/dimensions and watermark geometry/controls. |
| `pnpm test:unit content.test` after review | Passed, 1 file / 7 cases | Explicit null column counts are rejected instead of silently defaulting. |
| `DOCN_WRITE_PDF_ARTIFACTS=1 pnpm test:pdf primitives` | Passed, 1 file / 3 cases, repeated after layout adjustment | Existing shared specimens retained; content example extended from one to two pages rather than creating a separate suite. Nine total pages across the three specimens. |
| Actual PDF inspection, Poppler at 90 dpi | Passed, both content-specimen pages | All five categories, neutral styling, blank fields, multiline address, matching row rules, two signer areas, single/inline lines and restrained marks. Final second page re-rendered and inspected after alignment correction. |
| `pnpm quality` | Passed | Formatting, lint and workspace/root types. Targeted formatting/lint rerun after the alignment and null-validation edits; build also checks final application types. |
| `pnpm test:consumers` | Passed, 1 orchestral case, 112.57 seconds | Official shadcn installs in two temporary projects with retained configuration and actual browser/Node PDFs after the registry stops. Six added modules explain exact source counts of 59/60. |
| `pnpm verify:registry` | Passed, 24 items / 5 assets | Current source closure regenerated and checked against the pinned official schema. |
| `pnpm build` / `pnpm verify:docs` | Passed, 30 pages / 10 documentation pages / 254 links | Static routes compile against the current expanded aggregate source closure; all existing local anchors/links resolve. |
| `git diff --check` | Passed | No whitespace errors. |

### Evidence and limits

The shared example `packages/documents/src/examples/primitive-content.tsx` now includes one-column reference fields, two-column contact fields with a wrapped address and blank telephone, three-column review fields with an incomplete final row, explicit required text, compact/regular badges, neutral and explicitly labeled notices, all three signature arrangements and repeated/one-time watermarks. The PDF test verifies every relevant label, two pages, physical text bounds, column alignment, matching dual-signer labels, no interactive field objects, no page-two annotations, DRAFT on both pages and SAMPLE only on page one. Existing links, local media, typography, pagination and flow-frame assertions still pass.

Generated artifacts (not committed): `.artifacts/l12/pdf/primitive-content.pdf`, `.artifacts/l12/rendered/primitive-content-s02d-{1,2}.png` and the existing external-consumer logs. The first visual inspection caught unequal bottom rules when a neighboring field contained two lines; the value area now grows to the row height. The inline signature label was aligned with its writing line. PDF tests and visual inspection were repeated after these corrections. Consumer dependency/configuration evidence preceded these style-only and null-input edits; the current registry was regenerated and verified afterward, without adding a redundant external-install matrix. The small Vite consumer still reports its existing large PDF-engine chunk warning; this story does not claim a bundle-size optimization.

Form rows, signature groups and fixed-page content require author qualification at the actual width/font; this story does not introduce an arbitrary text-measurement engine, clipping, font shrinking or automatic splitting of oversized unbreakable rows. Group titles stay with the first row. Watermark is currently a direct DocumentFrame child after body content, horizontal, 1–24 printable Latin characters, 12–48 pt and opacity 0.02–0.2. It does not support rotated marks, image marks, arbitrary Unicode shaping or fixed-format PageFrame placement. No claim of PDF/UA, secure signing, tax compliance, physical print contrast or printer calibration is made.

PDF and React skill guidance led to extending the existing real-PDF specimen and inspecting its actual raster output; shadcn guidance preserved the official source installation/configuration boundary. Responsive/keyboard/reduced-motion checks were not repeated because no site UI or interactive behavior changed; S01 evidence remains applicable. No template redesign, dependency, deployment, release, L12 PR or merge. Next story: **L12-S02e**, vector graph components. L12 remains **in progress**.

## S02e — vector graph components

Implementation, tests and evidence commit: `c98d5e096f8bf28c2a3170128c05e90c14499b39`, based on `576861945ba6d796e23e10bdbe64e1159f6cd625`. Checks below were executed on the story working tree before that commit; no remote CI or completed-lot claim is made.

Implemented after the maintainer requested continuation from S02d. The graph API decision was documented before implementation. Graph exposes bar, horizontal-bar, line, area, pie and donut variants from bounded, immutable categorical data. All drawing uses native PDF SVG paths, rectangles, lines and circles, with selectable labels. Existing PDF theme roles provide monochrome defaults; no site chart library, raster screenshot or new dependency is used.

The single-series contract preserves input order and unique labels. Cartesian scales include zero, handle signed values and use finite nice ticks; all-zero series expand to 0–1. Empty arrays produce a visible message. Pie/donut reject negatives, leave zero categories in the legend and handle zero totals and complete single-category circles/rings. Numbered sector leaders match numbered category/value/share legends without requiring color identification. Conservative text reservations and a minimum 2 pt positive-sector outer arc reject unreadable density rather than dropping, truncating or silently aggregating data.

Seven source modules separate input validation, geometry, layout, text and the Cartesian/circular compositions behind Graph. They join the existing aggregate registry; component-sized installation entries and clickable public gallery/detail pages remain S02g/S02h. Existing templates, shared primitive implementations and the site shell were not changed.

### Executed checks

| Check | Actual result | Evidence |
| --- | --- | --- |
| `pnpm test:unit graph.test measurement registry` | Passed, 3 files / 13 cases | Five focused graph cases cover data/type/dimension bounds, signed/flat/tiny/large scales, proportional bars, full-circle/ring paths, zero categories and density rejection. Existing geometry/source-independence and registry checks reused. |
| `pnpm test:unit graph.test` after review | Passed, 1 file / 5 cases | Final density envelope, soft-hyphen rejection and straight single-sector leaders. |
| `DOCN_WRITE_PDF_ARTIFACTS=1 pnpm test:pdf graph` | Passed, 1 combined case / 2 pages | All six nominal forms together, followed by distinct empty, zero, signed and whole-circle/ring examples. PDF.js verifies selectable content, bounds, values/percentages and native paths without image paint operations. |
| Actual PDF inspection, Poppler at 110 dpi | Passed, both final pages | Legible labels/legends, signed zero baseline, correct proportional geometry, numbered leaders, complete pie and open donut center. Re-rendered after font and density corrections. |
| `pnpm quality` | Passed on final runtime source | Formatting, lint and workspace/root TypeScript checks. |
| `pnpm verify:registry` | Passed, 24 items / 5 assets | Seven additional source modules are included in the pinned-schema-validated aggregate closure. |
| `pnpm test:consumers` | Passed, 1 orchestral case, 118.46 seconds | Official CLI installation in two temporary external projects, unchanged consumer configuration and actual browser/Node PDFs after the registry stops. Exact source counts are 66/67, accounting for seven new modules. |
| `pnpm build` / `pnpm verify:docs` | Passed, 30 pages / 10 documentation pages / 254 local links | Existing static routes, anchors and guides compile with the expanded source closure. |
| `git diff --check` | Passed | No whitespace errors. |

### Evidence, corrections and limits

`packages/documents/src/examples/primitive-graphs.tsx` is the compiled source for the two-page specimen. Its dedicated PDF case groups the six nominal variants and only the distinct boundary risks, rather than introducing a theme/locale/variant matrix or rerunning every template family. It asserts every chart title, all four categories in each nominal chart, six visible series legends, explicit percentages, zero-category legend entries, signed values, empty-state messages, physical content bounds and absence of raster image painting. Existing templates are not restyled or requalified by this story.

The first type check rejected SVG font properties passed as attributes; the API requires style properties. The subsequent PDF content test passed but visual inspection revealed oversized text and overlapping legends because SVG text did not inherit the container font. GraphText now sets the qualified font family, weight and size on every SVG text node. The PDF test additionally asserts the actual 7 pt caption height for category, legend and empty-state text, and both pages were rendered and inspected again. No threshold was weakened or reference image regenerated to conceal a defect. The final conservative width envelope also covers wider qualified Latin glyphs; two specimen titles were explicitly shortened to fit rather than silently clipped.

Generated artifacts (not committed): `.artifacts/l12/pdf/primitive-graphs.pdf`, `.artifacts/l12/rendered/primitive-graphs-{1,2}.png`, and the existing external-consumer logs. An optional font-inventory diagnostic could not run because `pdffonts.exe` is not bundled; no font-inventory result is claimed. Actual engine output, PDF.js text-size assertions and final Poppler rendering provide the executed typography evidence. The first raster invocation emitted display-font warnings; final raster invocations completed without warnings. The existing external Vite sample still warns about its large PDF-engine bundle; no bundle optimization is claimed.

The current graph API is one categorical series, not a multi-series, stacked, time, logarithmic or scatter engine. It supports short printable Latin labels and bounded finite numbers; numeric display rounds to six significant digits and percentages to one decimal. Graphs are non-breaking boxes, so authors must ensure their requested dimensions fit the containing frame. Dense labels or very small circular shares fail explicitly and require author changes. Defaults are grayscale, but physical print contrast and printer calibration remain unqualified. No financial accuracy/compliance, PDF/UA, CMYK or PDF/X claim is made.

PDF skill guidance made the raster review mandatory and directly led to the typography correction. React guidance kept modules focused, imports direct and geometry derived without effects/state; shadcn guidance retained the existing installation/configuration boundary. No new responsive, keyboard or reduced-motion checks were needed for this PDF-only change; S01 covers the unchanged site shell. No dependency, template redesign, deployment, release, L12 PR or merge. Next story: **L12-S02f**, validated vector barcodes. L12 remains **in progress**.

## S02f — validated vector barcodes

Implementation, tests and evidence commit: `57d83ba01c4c61a4531a5d5aa2589557edca432f`, based on `1cdfae95d0efa3e0627a680c30d37b7a5eadb70e`, after the maintainer requested continuation. Checks below ran on the story working tree before its commit. L12 remains in progress; no remote CI, PR, merge or release claim.

Barcode exposes exactly Code 128 and EAN-13. Printable ASCII/length bounds, supplied EAN check digits, supported formats, module widths, bar heights, quiet zones and readable-text density are validated before a symbol can render. A separate check-digit helper accepts 12 digits without silently changing a supplied payload. Native PDF rectangles preserve vector output; opaque black/white scan regions are independent of theme colors. Optional selectable text uses the existing PDF body font and a bounded caption size. EAN guard extensions remain separate from the caption.

### Dependencies and isolation

The [dependency decision](../DEPENDENCIES.md#vector-barcodes-l12-s02f) and [API contract](../specs/COMPONENT_CATALOG.md#s02f-barcode-api-decision) were written before installing the packages. Adopted JsBarcode 3.12.3 (MIT, no runtime dependencies), using its public object output, not private format imports or DOM rendering. Independent ZXing 0.23.0 (Apache-2.0) and its MIT / Apache-2.0-option transitives are test-only. Versions and integrity hashes are locked; project licensing remains undecided.

Two registry items are introduced early: `docn-theme-context` moves ownership of the unchanged shared provider; `docn-barcode` owns its two new implementation files. The legacy primitive barrel and template dependency lists do not import Barcode or install its encoder. Barcode's item depends only on core, themes and theme context, not the aggregate primitives, templates, render pipeline or site. This narrowly anticipates S02g to qualify real installation now; it does not complete S02g's component-wide distribution work.

Measured with the existing Rolldown 1.2.6 browser platform, ESM/minify output, Node gzip defaults:

| Entry | Minified bytes | Gzip bytes | Isolation |
| --- | ---: | ---: | --- |
| Exact JsBarcode npm public entry, before adoption | 64,160 | 11,186 | No runtime dependency; all upstream formats remain bundled although only two are exposed. |
| Final Barcode source, React/JSX runtime/react-pdf external | 68,504 | 13,039 | 54 encoder modules; zero decoder modules. |
| Existing typography source, same externals | 59,540 | 14,156 | Zero encoder or decoder modules. This is the existing source entry, not a new Text-only registry-install claim. |

The last two measurements use inputs `packages/documents/src/primitives/{barcode,typography}.tsx`, external IDs `react`, `react/jsx-runtime`, `@react-pdf/renderer`, and count module paths containing `jsbarcode` / `zxing`. They exclude the shared PDF engine, fonts and app runtime; they are not total application sizes. The existing consumer browser app still warns about its roughly 1.99 MB uncompressed PDF-engine bundle. No bundle threshold was raised or disabled.

### Executed checks

| Check | Actual result | Evidence |
| --- | --- | --- |
| `pnpm test:unit barcode.test registry` | Passed, 2 files / 8 cases | Four focused barcode cases; registry graph/import checks plus encoder isolation. Includes invalid check digits, Unicode/control/newline/FNC rejection, leading zeroes, physical floors/ceilings, quiet zones, guard bars and caption bounds. |
| `DOCN_WRITE_PDF_ARTIFACTS=1 pnpm test:pdf barcode` | Passed, one combined PDF case | Four symbols: nominal and minimum-module Code 128/EAN-13. Actual post-processed PDF rasterized at 216 dpi, then independently decoded by ZXing's format-specific readers. All payloads match, including leading zeroes and the symbol without a caption. |
| PDF content and raster review | Passed, one A4 page | PDF.js verifies selectable 7 pt captions, hidden caption absence and vector paths without raster image paints. Poppler 110 dpi output was visually inspected: readable values, guard extensions, white quiet regions and no overlap. |
| `pnpm quality` | Passed | Formatting, lint and root/workspace types; final narrowed validation changes also receive targeted lint/types and test reruns. |
| Official external consumer orchestration | Passed, one case / two environments | Final run 121.48 seconds (first run 163.16 seconds). The CLI explicitly installs a template and Barcode per project; 68 browser / 69 Node installed sources. An extra shared QA composition is copied separately and excluded from these counts; no production implementation is copied by the test. |
| External real-PDF verification | Passed in browser and Node | Existing template PDFs still render. The same four barcode payloads decode from both extra PDFs after the registry server is stopped. Only the local browser origin is requested. Consumer `components.json` bytes are unchanged; encoder version is exact and decoder is not installed. |
| `pnpm verify:registry` | Passed, 26 items / 5 local assets | Pinned official schema and graph checks. Project status was observed as In progress, matching the local lot state. |
| `pnpm build` / `pnpm verify:docs` | Passed, 30 static pages / 10 documentation pages / 254 links | Site remains unchanged. No gallery/component detail route is advertised by this story. |
| Exported site chunk marker audit / `git diff --check` | Passed | No `JsBarcode`, `CODE128_AUTO`, `ean128` or `zxing` markers in exported JavaScript chunks; no whitespace errors. This static marker check complements the direct source/module and registry-closure checks, not a universal future bundle guarantee. |

The final caption envelope additionally accounts for wide Latin letters at larger allowed font sizes; invalid null dimensions no longer fall back to defaults. The actual final PDF bytes remain identical to the first externally rendered Node specimen (SHA-256 `7ca9a2cbf7ddcc4f9c27d0087979a02bd93062907ac0713ca2e3300d51fe03b7`). No visual reference was replaced to hide a difference.

Generated evidence remains uncommitted: `.artifacts/l12/pdf/primitive-barcodes.pdf`, `.artifacts/l12/rendered/primitive-barcodes-1.png`, `.artifacts/consumers/{browser,node}-barcodes.pdf`, consumer logs and install state. The PDF skill required final-raster inspection; React guidance kept the encoder off the shared barrel and avoided derived state/effects; shadcn guidance preserved official installation and consumer configuration.

### Limits and next step

Decoding at one controlled raster resolution is not scanner/printer certification, a barcode verification grade, GS1 product assignment, GS1-128 support or a retail-certified EAN layout. EAN uses a separate complete caption line; other symbologies, application identifiers and FNC payloads are rejected. Authors must place the non-breaking box inside sufficient page space. No promise of automatic barcode sizing, arbitrary-caption wrapping, CMYK, PDF/X or PDF/UA. ZXing is a pinned maintenance-only test oracle, not a new scanner feature.

No template restyling, website UI change, remote document generation, new responsive interaction or release. Existing site keyboard/reduced-motion evidence remains applicable. Next: **L12-S02g**, individually installable component closures; working public preview/code pages remain **S02h**.

## S02g — individually installable PDF components

Implementation, tests and evidence commit: `2dada84ae1e9f8927c0bbbcce0c1b564be135399`. Base: `a3902150cd40d4f2fb69e46fa5566c6fc3c9327d`, after the maintainer requested continuation. Checks ran on the story working tree before that commit. L12 remains **in progress**, not `verified_local`, merged or released.

### Distribution and compatibility

The development registry exposes 28 component-sized items, with 60 total registry items and the same five local font/license assets. Small contract, physical geometry and font/asset support entries prevent basic components from depending on the full template render pipeline. `docn-text` resolves eight source files and only React, react-pdf and Zod runtime dependencies. Neither Text nor unrelated component entries install templates, site UI, Barcode, QRCode or PDF post-processing. Barcode and QRCode retain their own explicit encoder dependencies.

Grouped typography, layout, containers, annotations, pagination and page-region modules become compatibility reexports. Their public names, target paths and implementation bodies remain stable. The legacy primitive barrel and `docn-primitives` registry entry still work, without adding Barcode. The private workspace package exposes the same individual component entry paths; it is not published to npm. Exact React/Node/QRCode type packages already used by the workspace are now explicit registry development dependencies for strict consumers. No new runtime dependency or lockfile change.

Two compiled examples are opt-in registry blocks: Text with local font preparation, and a DocumentFrame composition with TableHeader/DataTable, Graph, Barcode and PageFooter. Their source is installed by the official CLI, not copied into consumers by the test. The earlier test-only barcode usage wrapper is replaced by these reusable examples; the focused S02f PDF decoder case remains intact. This keeps one external orchestration, rather than a new installation suite per component.

Source previews now use explicit item/target metadata validated against the installation closure. DataTable exposes its own source and relevant table helpers without fetching Text, core or the entire repository. Templates still show only their own files and direct family foundation. A single-file source uses the full code area without an empty tree; changing the selected item hides old source immediately. Existing highlighting, copy controls, scrollbar hiding and tree buttons are reused. The generator also marks whether font preparation code is installed, so standalone component instructions explicitly add it when needed. Detailed component pages remain S02h.

### Executed checks

| Check | Actual result | Evidence |
| --- | --- | --- |
| `pnpm test:unit registry content table` | Passed, 3 files / 15 cases | Existing content/table contracts and registry graph/import checks. The registry's four cases also passed again after adding rejection of preview files outside the selected dependency closure. |
| `pnpm test:components registry-source-panel` | Passed, 1 file / 4 cases | Legacy source/copy behavior, bounded DataTable helpers, no transitive repository fetch, switching items without stale source, and no tree for a single file. |
| `DOCN_WRITE_PDF_ARTIFACTS=1 pnpm test:pdf primitives` | Passed, 3 files / 5 cases | Existing content, frame, pagination, graph and barcode PDF specimens after the behavior-preserving split; barcode decoding remains covered. No visual threshold/reference change. |
| `pnpm quality` | Passed | Formatting, lint and workspace/root TypeScript. Final example/test changes also receive formatting, targeted lint, root types and build types. |
| `pnpm verify:registry` | Passed, 60 items / 5 assets | Pinned official schema, unique ownership, bounded relative imports, complete dependency closures and deterministic generation. |
| `pnpm test:consumers` | Passed, one orchestral case / two external projects, final run 146.32 seconds | Official shadcn 4.19.0 CLI installs a 16-file Text example in the browser and a 38-file rich composition in Node, then a legacy invoice into the same Node project. All three entries pass strict TypeScript, Vite builds and actual PDF generation. |
| Consumer independence and preservation | Passed | Registry is stopped during every render. Browser requests only its own origin. Existing `components.json`, non-`@` aliases, CSS and owned source remain byte-identical. The later invoice installation preserves the installed Text source and reuses prepared assets; no overwrite flag or initializer. |
| Actual PDF extraction and Poppler raster review at 110 dpi | Passed, three one-page outputs | Text preserves the accented name; composed document has column headings, three rows, vector graph, barcode caption and final page number; the legacy invoice preserves content and total. Final page images are inspected, not treated as proof from extraction alone. |
| `pnpm build` / `pnpm verify:docs` | Passed, 30 static pages / 10 documentation pages / 254 local links | No new route is advertised before S02h. |
| `E2E_USE_BUILD=1 pnpm test:e2e catalog.spec.ts --grep 'browses templates and opens their registry source'` | Passed, 1 Chromium journey | Existing gallery, clipboard, bounded code drawer and hidden scrollbar behavior remain functional against the checked static build. |
| `git diff --check` | Passed | No whitespace errors. |

### Corrections, evidence and limits

The first observed external render failure came from the new example using physical point widths where DataTable expects percentages totaling 100. Its columns now use 70/30, without changing or weakening the component's validation. The next orchestration reached the legacy add step and correctly encountered the asset installer's no-overwrite protection. The test now prepares each consumer's assets once and reuses them for later source additions; no overwrite bypass was added. Final raster review also identified missing column headings in the example. It now explicitly composes TableHeader, and the PDF extraction check requires both uppercase headings.

Ignored evidence: `.artifacts/consumers/run.log`, `install-state.json`, `{browser,component,node}-consumer.pdf`, `.artifacts/l12/rendered/consumer-{text,components,invoice}.png`, existing primitive PDFs, and the Chromium report. The browser consumer still emits Vite's large-chunk advisory because it includes the PDF engine; no warning limit or test threshold was raised. CLI output confirms the later invoice creates its missing sources and skips shared ones. This is an installation/compatibility test, not a claim that updating edited source never requires manual review.

The shadcn skill guided official registry metadata and unchanged consumer configuration. React guidance led to direct component imports, parallel supporting-file fetches and item-keyed source state. The PDF skill required raster inspection and led to adding the missing table headings. No new page-layout or animation interaction was introduced; earlier responsive, keyboard and reduced-motion evidence for the shell remains applicable. The source-panel component checks and existing Chromium source journey cover the changed browsing boundary.

No automatic CSS/OKLCH/font inheritance, extra CLI, public registry namespace/directory registration, template redesign, scanner/printer certification, PDF/X, CMYK or PDF/UA claim. No publication, L12 PR or merge. Next permissible story: **L12-S02h**, working component detail pages with actual PDF previews, props, source/install instructions, formats and theme examples.

## S02h — working component, format and theme documentation

Implementation and evidence are recorded in this story commit; the following tracking commit records its actual SHA. Base: `11491873291141ba975d4e8181e6b752bf2e6c9d`, after the maintainer requested continuation. L12 remains **in progress**, not `verified_local`, merged or released.

### Delivered surface

| Before | After |
| --- | --- |
| Nine inert names on the component index. | 28 real detail links, the same inventory in sidebar/search, and a small new-component indicator for Barcode. |
| Component installation existed only in the registry. | Each page has its actual PDF, Preview/Code tabs, enlarged multipage view, download, bounded source drawer, official installation command, usage guidance, exported props and limitations. |
| Formats and themes were described only in guides. | `/formats/` derives all 13 physical definitions and template compatibility from contracts/catalog; `/themes/` renders identical content with Neutral, Editorial and Bold. |
| The mobile navigation contained a short list. | The existing single menu scrolls to all component links without visible scrollbars; the approved desktop/tablet sidebar breakpoints remain unchanged. |

The typed example functions are the source for both rendered output and displayed usage. TypeScript extraction retains referenced local types, constants and imports, omits unrelated examples, and rewrites document imports to the installed `docn/` tree. Installation distinguishes the single component from the additional helpers used by its example. Font preparation remains explicit and links to the qualified asset guide; nothing initializes or overwrites the user's shadcn configuration.

API tables read public function props and parameter defaults using the TypeScript checker, including compatibility aliases and the four composable Table exports. Defaults implemented in downstream resolvers are not invented; the page explains the table's scope and links to complete source. Types are printed in their own declaration context, without leaking local absolute paths. Examples require the documented PDF page/theme wrapper or return their own frame; they are not executable user-entered code.

Build/predev generate a server-side documentation manifest, 31 actual PDFs and 32 page rasters using existing react-pdf, PDF.js and canvas dependencies. The manifest includes output hashes and input-based caching. PDF rendering/fonts do not enter the documentation client bundle: the client receives only preview paths/text, and source is fetched on demand by the existing bounded registry viewer. No dependency or lockfile change, production primitive change, template redesign or second document engine.

### Executed checks

| Check | Actual result |
| --- | --- |
| `pnpm generate:docs` and production prebuild | Passed: 28 component PDFs, 3 theme PDFs, 32 nonempty rasterized pages, 13 format definitions; inventory exactly matches the component registry. |
| `pnpm test:unit source-reference` | Passed, 1 file / 2 cases: catalog-registry-sidebar-search parity and usage extraction retaining DataTable types/data while excluding unrelated examples. |
| `pnpm test:components shell-navigation` | Passed, existing 1 file / 2 cases: single menu, keyboard shortcuts, search and focus behavior after inventory expansion. |
| `pnpm quality` | Passed: formatting, lint, workspace and root TypeScript. All new example functions compile against the actual component APIs. |
| `pnpm build` | Passed: 60 static pages, including all 28 component details, formats and themes. Registry still has 60 entries / 5 verified assets. |
| `pnpm verify:docs` | Passed: 41 documentation pages and 2,427 local links, including anchors, format/template links and downloadable PDFs. |
| `E2E_USE_BUILD=1 pnpm test:e2e catalog.spec.ts --grep 'browses component examples source formats and themes'` | Passed: 1 Chromium journey, 12.0 seconds; actual preview load, Code tab, bounded source, origin-aware install command, API, search, second PDF page, enlarged keyboard scrolling, focus restoration, tablet sidebars, one mobile menu, formats/themes and last-component navigation. |
| Visual review | All 31 specimens reviewed in four contact sheets; Graph and PageBreak page 2 independently rasterized with Poppler. Actual browser Barcode preview/source and final desktop, mobile, theme and enlarged-page screenshots inspected. No clipping, overlapping controls, visible scrollbars or horizontal page overflow observed in the reviewed states. |
| `git diff --check` | Passed. |

### Corrections and boundaries

Initial example generation correctly rejected a custom flow format and undersized margins. Examples now use supported portrait A4 with 36-point margins; runtime validation was not broadened. PageBreak explicitly wraps its next-page content. Browser inspection exposed TypeScript's absolute-path-qualified alias formatting; printing from the property's declaration context fixes it, and generation rejects workspace paths in the public component data. Lint caught two JSX apostrophes, corrected before the passing quality run. No deliberately failing commit or weakened assertion.

Ignored evidence: `.artifacts/docs/catalog.json`, `contact-{0,8,16,24}.png`, `graph-poppler.png`, `page-break-poppler.png`; `apps/www/public/generated/docs/` contains the real PDFs/rasters; `.artifacts/e2e/test-results/catalog-browses-component-examples-source-formats-and-themes/` contains desktop, mobile, themes and enlarged-page captures. These files are regenerable, not committed screenshots or invented proof.

Impeccable and interface-polish guidance kept the existing identity, quiet component index and shared code blocks; React guidance kept generation on the build side, state local and preview props small. The PDF skill required real raster review. Existing Base UI Tabs, Dialog and Sheet provide keyboard interaction; the new viewer retains hidden scrollbars, focusable scrolling and reduced-motion behavior. The visible ring on focused scrolling regions is intentional keyboard feedback, not a permanent decorative border.

External installation evidence from S02g remains applicable: no registry source/dependency or production primitive behavior changed here. No installation per documentation card, full PDF matrix or unrelated suite rerun. The examples are demonstrations, not print/scanner certification, accessible-PDF certification or new templates. The local development registry is still mutable. No automatic CSS/font inheritance, release, deployment, PR or merge.

Next permissible story: **L12-S03**, contribution/review guidance and final search/metadata/canonical/sitemap rules, including non-indexable preview URLs. The component pages are real static HTML; final SEO governance is not claimed complete in S02h.
