# QA — L01 — Reproducible bootstrap and quality scopes

Date: 2026-08-29. Status: **in progress**. Branch `chore/bootstrap-workspace`; base `07dd743db42c293ed59f99e6ff734eaef94ea86b`. Issue [#3](https://github.com/Osiris-Balonga/docn-ui/issues/3).

## Scope

Two private workspaces, static Next.js export, shadcn Base UI, separate lightweight test scopes, and focused CI. No PDF generation, catalog, hosting, or new license decision.

## Environment and decisions

Windows / PowerShell; Node 24.18.0. The host's pnpm 11.19.0 is older than the project pin, so commands use `npm exec --yes --package=pnpm@11.24.0 -- pnpm <command>` without changing the global toolchain. This is the same pinned pnpm used by the package scripts and CI.

The initial direct `pnpm install` correctly rejected the mismatched engine before installing. Version resolution and dependency licenses are recorded in [DEPENDENCIES](../DEPENDENCIES.md). No application tests exist at the start of S01; installation, compiler, static build, and browser inspection provide its evidence.

## Results

### S01 — workspace

- `pnpm install --frozen-lockfile`: passed with pnpm 11.24.0; no lockfile changes.
- `pnpm typecheck`: passed for both workspaces, including generated static route types.
- `pnpm build`: passed with Next 16.3.3 / Turbopack; `/` and `/_not-found` prerendered, `out/index.html` and `out/404.html` produced.
- `pnpm preview`: served only the export at loopback port 4173 (checked unoccupied before startup). HTTP `/` = 200, `/not-a-route/` = 404 with custom recovery content.
- Actual browser DOM inspection confirmed the heading, honest unavailable-template status, and the correct repository link. No application suite added for scaffolding.
- Network download retries occurred during initial installation; the completed frozen-lockfile pass succeeded. The http-server transitive `whatwg-encoding@2.0.0` is deprecated; it is a development-server dependency, not browser code.

S01: `897e9cce5c0add3fd1bc4fe757275ed0ec5f173b`, pushed; issue checkbox/evidence read back.

### S02 — shadcn and local fonts

- Exact CLI 4.19.0 help inspected. Noninteractive Base UI init completed once; Button generated, Tooltip added, no force/overwrite.
- Production build and included TypeScript check passed. Browser screenshot reviewed: restrained neutral page, readable type, working shadcn action. Computed body font is `Geist, ui-sans-serif, system-ui, sans-serif`.
- Export contains the two WOFF2 assets at their original 69652 / 71368 byte sizes. Source hashes and OFL are tracked. Removed CLI-generated Google import and circular font variable.
- Browser inspection caught a link announced as a button. Explicit link semantics corrected in the composition; subsequent DOM readback reports the repository link correctly. Keyboard interaction exposes the tooltip and the action navigates to the actual repository.
- No UI variant matrix or application tests yet. The composition regression is the one meaningful smoke check to add in S03. S03/S04 remain unverified.

S02: `a5ea60d0b03af4ab53e280a21e69324b15cfce1c`, pushed; issue checklist and evidence read back.

### S03 — separate quality scopes

- One component test covers our composed action's link role, repository URL, keyboard focus, and Tooltip integration. No tests for shadcn variants, constants, or empty document modules.
- Vitest collection lists the single file only under `components`. `test:unit` and `test:integration` each return exit 1 with "No test files found", as intended for unimplemented scopes; neither is claimed passing.
- `test:coverage`: 1 file / 1 test passed; local report generated under `.artifacts/coverage`. This one-time run checks the new command contract, not a percentage target.
- `test:all --list`: only `test` activated; PDF/consumers/E2E/visual explicitly unavailable. `test:all` uses a sequential child process and propagates nonzero exit status.
- `pnpm peers check`: no conflicts after retaining compatible ESLint 9.39.5. ESLint 10.9.1 failed on `react/display-name`; no lint rules disabled. Deprecation tracked in DEPENDENCIES for L13.
- Initial typecheck caught missing matcher augmentation; explicit test matcher types now resolve and both workspaces plus tooling typecheck pass. An anonymous PostCSS export was named to satisfy the existing lint rule.
- Frozen-lockfile installation passed after explicitly reviewing/allowing the unrs-resolver postinstall. No blanket build-script permission.
- Prettier normalized existing code/config whitespace, including governance sources and ruleset JSON. Their semantics are unchanged; the separate native policy test still passes (one table, 24 cases). Markdown/history were not reformatted.

Final S03 `pnpm validate`: **passed** (formatting, zero-warning lint, workspace/tooling types, then one file / one component test). Per-project listings returned `[]`, the one component file, `[]`; no duplicate collection. S04 CI/rules updates remain pending.

S03: `cead66f0a47857afcdeb764a0123359472d7c8e7`. Post-commit tracking caught that the verified `eslint.config.mjs` was accidentally omitted from staging. A separate `fix(tooling): include the verified ESLint configuration` commit adds the exact configuration used by the passing validation; no history rewrite or rule change. Both commits belong to S03's evidence; do not use the first SHA alone as a complete bootstrap.

### S04 — CI preparation

Three PR jobs: `quality` (format/lint/types), `unit-tests` (the three lightweight projects in one run; currently one real component test), and `build` (static export). Read-only token, checkout credentials disabled, per-PR concurrency, explicit Node/pnpm pins, 15-minute timeouts. No push/deployment trigger, Project secret, heavy suite, or duplicate full validation.

Action provenance read through the upstream GitHub API: checkout v4 → `11d5960a326750d5838078e36cf38b85af677262`; setup-node v4 → `49933ea5288caeca8642d1e84afbd3f7d6820020`; pnpm/action-setup v4 annotated tag resolves to commit `b906affcce14559ad1aafd4ab0e942779e9f58b1`. The latter reads packageManager from package.json.

Remote check results and additional required-check rules will be recorded only after an actual successful PR run. Branch-policy remains unchanged and required throughout.

Local build after S03 and CI preparation: passed (Next static export and build-time TypeScript). S03's successful validation is reused for unchanged application/test sources; CI will validate the committed tree independently.
