# QA — L08 — event tickets and vector QR

Date: 2026-08-30. Status: **in progress**. Branch `feat/event-ticket-templates`, base `21d6a6f0025a89eeb28ef2058b272216a91607f3`. Issue [#12](https://github.com/Osiris-Balonga/docn-ui/issues/12), Project item `PVTI_lAHOCZDjCM4BhyZuzg4fVew` in `In progress`. No pull request exists.

## Scope

L08 adds a strict event-ticket data contract, a print-qualified vector QR primitive, and three distinct ticket compositions. Automated evidence must decode the QR from a rasterization of the final PDF and preserve the selected IANA time zone. This is a data carrier and graphic access marker, not a secure ticketing claim.

## Verification

| Command or check | Environment | Actual result | Evidence |
| --- | --- | --- | --- |
| L07 merge and L08 base observation | GitHub PR/API, Project API, and local Git | Passed | PR #27 merged at `2026-08-30T14:51:33Z`; issue #10 closed; L07 moved to `Done`; L08 moved to `In progress`; the branch starts from merge `21d6a6f0025a89eeb28ef2058b272216a91607f3` |

## Justification for added tests

S01 will add date/payload unit coverage and one actual-PDF raster/decode case because existing tests verify only QR matrix construction and visual presence, not independent decoding from final PDF bytes. Later stories will extend one ticket-family PDF suite rather than creating one suite per composition.

## Visual and document checks

Not yet executed. L08 requires one selected visual reference, a three-composition contact sheet, final-PDF QR decoding, and an explicit distinction between automated digital decoding and a later physical print trial.

## Deviations and resumption

The maintained `qrcode` dependency and vector primitive already exist from L05. L08 will strengthen that shared primitive instead of adding a competing encoder or runtime dependency. S01 is next. No local verification, PR, merge, publication, or physical print evidence exists yet.
